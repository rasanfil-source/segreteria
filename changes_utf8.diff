diff --git a/gas_email_processor.js b/gas_email_processor.js
index 73deaab..6133a15 100644
--- a/gas_email_processor.js
+++ b/gas_email_processor.js
@@ -3,12 +3,12 @@
  * 
  * PIPELINE ELABORAZIONE:
  * 1. FILTRA: Dobbiamo processare questa email?
- * 2. CLASSIFICA: Che tipo di richiesta ├¿?
+ * 2. CLASSIFICA: Che tipo di richiesta ├â┬¿?
  * 3. GENERA: Crea risposta AI
- * 4. VALIDA: Controlla qualit├á risposta
+ * 4. VALIDA: Controlla qualit├â┬á risposta
  * 5. INVIA: Rispondi all'email
  * 
- * FUNZIONALIT├Ç AVANZATE:
+ * FUNZIONALIT├âÔé¼ AVANZATE:
  * - Lock a livello thread (anti race condition)
  * - Anti-loop detection
  * - Salutation mode (full/soft/none_or_continuity/session)
@@ -113,7 +113,7 @@ class EmailProcessor {
    * @param {GmailThread} thread 
    * @param {string} knowledgeBase - KB testo semplice
    * @param {Array} doctrineBase - KB strutturata
-   * @param {Set} labeledMessageIds - ID messaggi gi├á etichettati (opzionale)
+   * @param {Set} labeledMessageIds - ID messaggi gi├â┬á etichettati (opzionale)
    * @param {boolean} skipLock - Se true, salta acquisizione lock
    */
   processThread(thread, knowledgeBase, doctrineBase, labeledMessageIds = new Set(), skipLock = false) {
@@ -132,9 +132,9 @@ class EmailProcessor {
     let lockValue = null;
 
     if (skipLock) {
-      console.log(`­ƒöÆ Lock saltato per thread ${threadId} (chiamante ha gi├á lock)`);
+      console.log(`├░┼©ÔÇØÔÇÖ Lock saltato per thread ${threadId} (chiamante ha gi├â┬á lock)`);
     } else if (!scriptCache || typeof LockService === 'undefined' || !LockService || typeof LockService.getScriptLock !== 'function') {
-      console.warn(`ÔÜá´©Å Lock service/cache non disponibili per thread ${threadId}: procedo senza lock`);
+      console.warn(`├ó┼í┬á├»┬©┬Å Lock service/cache non disponibili per thread ${threadId}: procedo senza lock`);
     } else {
       const configuredTtl = (typeof CONFIG !== 'undefined' && Number(CONFIG.CACHE_LOCK_TTL))
         ? Number(CONFIG.CACHE_LOCK_TTL)
@@ -150,7 +150,7 @@ class EmailProcessor {
       const scriptLock = LockService.getScriptLock();
       try {
         if (!scriptLock.tryLock(5000)) {
-          console.warn(`­ƒöÆ Impossibile acquisire lock globale per thread ${threadId}, salto`);
+          console.warn(`├░┼©ÔÇØÔÇÖ Impossibile acquisire lock globale per thread ${threadId}, salto`);
           return { status: 'skipped', reason: 'global_lock_unavailable' };
         }
 
@@ -160,10 +160,10 @@ class EmailProcessor {
           const isStale = !isNaN(existingTimestamp) && (Date.now() - existingTimestamp) > lockTtlMs;
 
           if (isStale) {
-            console.warn(`­ƒöô Lock stale rilevato per thread ${threadId}, pulizia`);
+            console.warn(`├░┼©ÔÇØÔÇ£ Lock stale rilevato per thread ${threadId}, pulizia`);
             scriptCache.remove(threadLockKey);
           } else {
-            console.warn(`­ƒöÆ Thread ${threadId} lockato da altro processo, salto`);
+            console.warn(`├░┼©ÔÇØÔÇÖ Thread ${threadId} lockato da altro processo, salto`);
             return { status: 'skipped', reason: 'thread_locked' };
           }
         }
@@ -171,14 +171,14 @@ class EmailProcessor {
         scriptCache.put(threadLockKey, lockValue, ttlSeconds);
         const confirmValue = scriptCache.get(threadLockKey);
         if (confirmValue !== lockValue) {
-          console.warn(`­ƒöÆ Collisione lock cache su thread ${threadId}, salto`);
+          console.warn(`├░┼©ÔÇØÔÇÖ Collisione lock cache su thread ${threadId}, salto`);
           return { status: 'skipped', reason: 'thread_lock_collision' };
         }
 
         lockAcquired = true;
-        console.log(`­ƒöÆ Lock acquisito per thread ${threadId}`);
+        console.log(`├░┼©ÔÇØÔÇÖ Lock acquisito per thread ${threadId}`);
       } catch (e) {
-        console.warn(`ÔÜá´©Å Errore acquisizione lock thread: ${e.message}`);
+        console.warn(`├ó┼í┬á├»┬©┬Å Errore acquisizione lock thread: ${e.message}`);
         return { status: 'error', error: 'Lock acquisition failed' };
       } finally {
         if (scriptLock && typeof scriptLock.releaseLock === 'function') {
@@ -196,7 +196,7 @@ class EmailProcessor {
       error: null
     };
 
-    // Snapshot robusto del classificatore errori ÔÇö restituisce sempre { type, retryable, message }
+    // Snapshot robusto del classificatore errori ├óÔé¼ÔÇØ restituisce sempre { type, retryable, message }
     // per coerenza con il contratto di gas_error_types.classifyError
     const classifyError = (typeof this._classifyError === 'function')
       ? this._classifyError.bind(this)
@@ -221,7 +221,7 @@ class EmailProcessor {
           myEmail = activeUser ? activeUser.getEmail() : '';
         }
       } catch (e) {
-        console.warn(`ÔÜá´©Å Impossibile recuperare email utente: ${e.message}`);
+        console.warn(`├ó┼í┬á├»┬©┬Å Impossibile recuperare email utente: ${e.message}`);
       }
 
       if (!myEmail) {
@@ -240,9 +240,9 @@ class EmailProcessor {
         myEmail = adminEmail || botEmailProperty || botEmailConfig || '';
 
         if (myEmail) {
-          console.warn(`ÔÜá´©Å Session email non disponibile: uso fallback anti-loop (${myEmail})`);
+          console.warn(`├ó┼í┬á├»┬©┬Å Session email non disponibile: uso fallback anti-loop (${myEmail})`);
         } else {
-          console.warn('ÔÜá´©Å Session email non disponibile e nessun fallback configurato (ScriptProperties.ADMIN_EMAIL/CONFIG.LOGGING.ADMIN_EMAIL/BOT_EMAIL/CONFIG.BOT_EMAIL)');
+          console.warn('├ó┼í┬á├»┬©┬Å Session email non disponibile e nessun fallback configurato (ScriptProperties.ADMIN_EMAIL/CONFIG.LOGGING.ADMIN_EMAIL/BOT_EMAIL/CONFIG.BOT_EMAIL)');
         }
       }
 
@@ -267,7 +267,7 @@ class EmailProcessor {
       });
 
       const externalUnread = unlabeledUnread.filter(message => {
-        // B07: Usa getFrom() leggero anzich├® la costosa extractMessageDetails()
+        // B07: Usa getFrom() leggero anzich├â┬® la costosa extractMessageDetails()
         const rawFrom = (message.getFrom() || '');
         const senderEmail = this.gmailService._extractEmailAddress(rawFrom);
 
@@ -276,12 +276,12 @@ class EmailProcessor {
         return !ownAddresses.has(senderEmail.toLowerCase());
       });
 
-      // GUARDRAIL (critico): se un messaggio ├¿ gi├á stato etichettati IA, non deve
-      // rientrare nel ciclo di risposta automatica anche se il thread ├¿ ancora aperto.
+      // GUARDRAIL (critico): se un messaggio ├â┬¿ gi├â┬á stato etichettati IA, non deve
+      // rientrare nel ciclo di risposta automatica anche se il thread ├â┬¿ ancora aperto.
       // Questo evita doppie risposte su stesso messaggio.
-      // Se non ci sono messaggi non letti non ancora etichettati ÔåÆ skip
+      // Se non ci sono messaggi non letti non ancora etichettati ├óÔÇáÔÇÖ skip
       if (unlabeledUnread.length === 0) {
-        console.log('   Ôèû Thread gi├á elaborato (nessun nuovo messaggio non letto)');
+        console.log('   ├ó┼áÔÇô Thread gi├â┬á elaborato (nessun nuovo messaggio non letto)');
         result.status = 'skipped';
         result.reason = 'already_labeled_no_new_unread';
         return result;
@@ -289,9 +289,9 @@ class EmailProcessor {
 
       // GUARDRAIL (critico): rispondiamo solo a mittenti esterni.
       // I messaggi interni (noi/alias) vengono esclusi per evitare loop e risposte non dovute.
-      // Se non ci sono messaggi da esterni ÔåÆ skip
+      // Se non ci sono messaggi da esterni ├óÔÇáÔÇÖ skip
       if (externalUnread.length === 0) {
-        console.log('   Ôèû Saltato: nessun nuovo messaggio esterno non letto');
+        console.log('   ├ó┼áÔÇô Saltato: nessun nuovo messaggio esterno non letto');
         unlabeledUnread.forEach(message => this._markMessageAsProcessed(message, labeledMessageIds));
         result.status = 'skipped';
         result.reason = 'no_external_unread';
@@ -302,12 +302,12 @@ class EmailProcessor {
       candidate = externalUnread[externalUnread.length - 1];
       const messageDetails = this.gmailService.extractMessageDetails(candidate);
 
-      console.log(`\n­ƒôº Elaborazione: ${(messageDetails.subject || '').substring(0, 50)}...`);
+      console.log(`\n├░┼©ÔÇ£┬º Elaborazione: ${(messageDetails.subject || '').substring(0, 50)}...`);
       console.log(`   Da: ${messageDetails.senderEmail} (${messageDetails.senderName})`);
 
 
       if (messageDetails.isNewsletter) {
-        console.log('   Ôèû Saltato: rilevata newsletter (List-Unsubscribe/Precedence)');
+        console.log('   ├ó┼áÔÇô Saltato: rilevata newsletter (List-Unsubscribe/Precedence)');
         this._markMessageAsProcessed(candidate, labeledMessageIds);
         result.status = 'filtered';
         result.reason = 'newsletter_header';
@@ -316,7 +316,7 @@ class EmailProcessor {
 
       // ====================================================================================================
       // STEP 0: CONTROLLO ULTIMO MITTENTE (Anti-Loop & Ownership)
-      // GUARDRAIL (critico): se l'ultimo intervento nel thread ├¿ nostro,
+      // GUARDRAIL (critico): se l'ultimo intervento nel thread ├â┬¿ nostro,
       // il bot NON deve inviare un'altra risposta. Si attende il prossimo messaggio utente.
       // ====================================================================================================
       const lastMessage = messages[messages.length - 1];
@@ -332,8 +332,8 @@ class EmailProcessor {
       );
 
       if (lastSpeakerIsUs) {
-        console.log('   Ôèû Saltato: l\'ultimo messaggio del thread ├¿ gi├á nostro (bot o segreteria)');
-        // Non marchiamo nulla, semplicemente ci fermiamo finch├® l'utente non risponde
+        console.log('   ├ó┼áÔÇô Saltato: l\'ultimo messaggio del thread ├â┬¿ gi├â┬á nostro (bot o segreteria)');
+        // Non marchiamo nulla, semplicemente ci fermiamo finch├â┬® l'utente non risponde
         result.status = 'skipped';
         result.reason = 'last_speaker_is_me';
         return result;
@@ -345,7 +345,7 @@ class EmailProcessor {
       const safeSenderEmail = (messageDetails.senderEmail || '').toLowerCase();
 
       // Verifica mittente: confronta solo con email proprie e alias configurati.
-      // Il confronto con i destinatari (To/CC) ├¿ stato rimosso perch├® genera
+      // Il confronto con i destinatari (To/CC) ├â┬¿ stato rimosso perch├â┬® genera
       // falsi positivi su mailing list e thread con CC multipli.
       const isMe = Boolean(safeSenderEmail) && (
         (Boolean(normalizedMyEmail) && safeSenderEmail === normalizedMyEmail) ||
@@ -353,7 +353,7 @@ class EmailProcessor {
       );
 
       if (isMe) {
-        console.log('   Ôèû Saltato: messaggio auto-inviato (o da alias conosciuto)');
+        console.log('   ├ó┼áÔÇô Saltato: messaggio auto-inviato (o da alias conosciuto)');
         this._markMessageAsProcessed(candidate, labeledMessageIds);
         result.status = 'skipped';
         result.reason = 'self_sent';
@@ -383,7 +383,7 @@ class EmailProcessor {
         /auto-reply|autoreply/i.test(xAutoReply) ||
         /oof|all|dr|rn|nri|auto/i.test(xAutoResponseSuppress)
       ) {
-        console.log('   Ôèû Saltato: risposta automatica (header SMTP)');
+        console.log('   ├ó┼áÔÇô Saltato: risposta automatica (header SMTP)');
         this._markMessageAsProcessed(candidate, labeledMessageIds);
         result.status = 'filtered';
         result.reason = 'out_of_office';
@@ -401,7 +401,7 @@ class EmailProcessor {
       const oooSubject = messageDetails.subject || '';
       const oooBody = messageDetails.body || '';
       if (outOfOfficePatterns.some(p => p.test(`${oooSubject} ${oooBody}`))) {
-        console.log('   Ôèû Saltato: risposta automatica out-of-office (testo)');
+        console.log('   ├ó┼áÔÇô Saltato: risposta automatica out-of-office (testo)');
         this._markMessageAsProcessed(candidate, labeledMessageIds);
         result.status = 'filtered';
         result.reason = 'out_of_office';
@@ -424,7 +424,7 @@ class EmailProcessor {
           /\b(grazie|ok|perfetto)\b/i.test(candidateBody);
 
         if (previousIsUs && arrivedSoonAfterUs && isShortClosureReply) {
-          console.log('   Ôèû Saltato: risposta breve di chiusura (grazie/ok/perfetto)');
+          console.log('   ├ó┼áÔÇô Saltato: risposta breve di chiusura (grazie/ok/perfetto)');
           this._markMessageAsProcessed(candidate, labeledMessageIds);
           result.status = 'filtered';
           result.reason = 'short_closure_reply';
@@ -440,7 +440,7 @@ class EmailProcessor {
 
       if (messages.length > MAX_THREAD_LENGTH) {
         if (!myEmail) {
-          console.warn('   ÔÜá´©Å Email utente non disponibile: skip controllo anti-loop basato su mittente');
+          console.warn('   ├ó┼í┬á├»┬©┬Å Email utente non disponibile: skip controllo anti-loop basato su mittente');
         }
         let consecutiveExternal = 0;
         const knownAliases = (typeof CONFIG !== 'undefined' && Array.isArray(CONFIG.KNOWN_ALIASES))
@@ -463,14 +463,14 @@ class EmailProcessor {
         }
 
         if (normalizedMyEmail && consecutiveExternal >= MAX_CONSECUTIVE_EXTERNAL) {
-          console.log(`   Ôèû Saltato: probabile loop email (${consecutiveExternal} esterni consecutivi)`);
+          console.log(`   ├ó┼áÔÇô Saltato: probabile loop email (${consecutiveExternal} esterni consecutivi)`);
           this._markMessageAsProcessed(candidate, labeledMessageIds);
           result.status = 'filtered';
           result.reason = 'email_loop_detected';
           return result;
         }
 
-        console.warn(`   ÔÜá´©Å Thread lungo (${messages.length} messaggi) ma non loop - elaboro`);
+        console.warn(`   ├ó┼í┬á├»┬©┬Å Thread lungo (${messages.length} messaggi) ma non loop - elaboro`);
       }
 
       // ====================================================================================================
@@ -479,7 +479,7 @@ class EmailProcessor {
       const senderInfo = `${messageDetails.senderEmail} ${messageDetails.senderName}`.toLowerCase();
       const autoPattern = /no-reply|do-not-reply|noreply|daemon|postmaster|bounce|mailer/i;
       if (autoPattern.test(senderInfo)) {
-        console.log('   Ôèû Saltato: mittente rilevato come casella automatica o no-reply');
+        console.log('   ├ó┼áÔÇô Saltato: mittente rilevato come casella automatica o no-reply');
         this._markMessageAsProcessed(candidate, labeledMessageIds);
         result.status = 'filtered';
         result.reason = 'no_reply_sender';
@@ -490,7 +490,7 @@ class EmailProcessor {
       // STEP 1: FILTRO - Domini/parole chiave ignorati
       // ====================================================================================================
       if (this._shouldIgnoreEmail(messageDetails)) {
-        console.log('   Ôèû Filtrato: domain/keyword ignore');
+        console.log('   ├ó┼áÔÇô Filtrato: domain/keyword ignore');
         this._markMessageAsProcessed(candidate, labeledMessageIds);
         result.status = 'filtered';
         return result;
@@ -511,7 +511,7 @@ class EmailProcessor {
       );
 
       if (!classification.shouldReply) {
-        console.log(`   Ôèû Filtrato dal classifier: ${classification.reason}`);
+        console.log(`   ├ó┼áÔÇô Filtrato dal classifier: ${classification.reason}`);
         this._markMessageAsProcessed(candidate, labeledMessageIds);
         result.status = 'filtered';
         return result;
@@ -526,7 +526,7 @@ class EmailProcessor {
       );
 
       if (!quickCheck.shouldRespond) {
-        console.log(`   Ôèû Gemini quick check: nessuna risposta necessaria (${quickCheck.reason})`);
+        console.log(`   ├ó┼áÔÇô Gemini quick check: nessuna risposta necessaria (${quickCheck.reason})`);
         this._markMessageAsProcessed(candidate, labeledMessageIds);
         result.status = 'filtered';
         return result;
@@ -537,7 +537,7 @@ class EmailProcessor {
         messageDetails.subject
       ) || {};
       const detectedLanguage = (quickCheck.language || languageDetection.lang || 'it').toLowerCase();
-      console.log(`   ­ƒîÉ Lingua: ${detectedLanguage.toUpperCase()}`);
+      console.log(`   ├░┼©┼Æ┬É Lingua: ${detectedLanguage.toUpperCase()}`);
 
       // ====================================================================================================
       // STEP 4: CLASSIFICAZIONE TIPO RICHIESTA (Multi-dimensionale)
@@ -550,7 +550,7 @@ class EmailProcessor {
 
       // Estrai dati dalla nuova struttura classificazione
       const categoryHint = this.requestClassifier.getRequestTypeHint(requestType);
-      const isPastoral = requestType.dimensions ? (requestType.dimensions.pastoral > 0.6) : (requestType.type === 'pastoral'); // Compatibilit├á
+      const isPastoral = requestType.dimensions ? (requestType.dimensions.pastoral > 0.6) : (requestType.type === 'pastoral'); // Compatibilit├â┬á
 
       // ====================================================================================================
       // STEP 5: KB ENRICHMENT CONDIZIONALE
@@ -567,18 +567,18 @@ class EmailProcessor {
       knowledgeSections.push('--- ISTRUZIONI OPERATIVE ---\n' + knowledgeBase);
 
       // AI_CORE: Solo per situazioni pastorali o complesse
-      // Nota: complexity pu├▓ essere undefined/null in classificazioni parziali.
+      // Nota: complexity pu├â┬▓ essere undefined/null in classificazioni parziali.
       // Usiamo Number()+fallback per evitare confronti silenziosi non intenzionali.
       const complexityScore = Number(requestType && requestType.complexity);
       const safeComplexity = Number.isFinite(complexityScore) ? complexityScore : 0;
       if (aiCore && (isPastoral || safeComplexity > 0.7)) {
-        console.log('   ­ƒºá AiCore (Avanzata) iniettata per contesto pastorale/complesso');
+        console.log('   ├░┼©┬º┬á AiCore (Avanzata) iniettata per contesto pastorale/complesso');
         knowledgeSections.push('--- LOGICA AVANZATA ---\n' + aiCore);
       }
 
       // Dottrina: Solo per domande dottrinali
-      if (effectiveDoctrineBase && (requestType.type === 'spiritual' || requestType.category === 'doctrine')) {
-        console.log('   ­ƒôû Dottrina iniettata per domanda spirituale');
+      if (effectiveDoctrineBase && (requestType.needsDoctrine || requestType.type === 'doctrinal')) {
+        console.log('   ├░┼©ÔÇ£ÔÇô Dottrina iniettata per domanda spirituale');
         knowledgeSections.push('--- FONTE DOTTRINALE ---\n' + effectiveDoctrineBase);
       }
 
@@ -586,7 +586,7 @@ class EmailProcessor {
       if (typeof getSpecialMassTimeRule === 'function') {
         const specialMassRule = getSpecialMassTimeRule(new Date());
         if (specialMassRule) {
-          console.log('   ­ƒÜ¿ Regola Messe Speciali iniettata nel Prompt');
+          console.log('   ├░┼©┼í┬¿ Regola Messe Speciali iniettata nel Prompt');
           knowledgeSections.push('--- ECCEZIONI CALENDARIO ---\n' + specialMassRule);
         }
       }
@@ -616,7 +616,7 @@ class EmailProcessor {
       const memoryContext = this.memoryService.getMemory(threadId) || {};
 
       if (Object.keys(memoryContext).length > 0) {
-        console.log(`   ­ƒºá Memoria trovata: lang=${memoryContext.language}, topics=${(memoryContext.providedInfo || []).length}`);
+        console.log(`   ├░┼©┬º┬á Memoria trovata: lang=${memoryContext.language}, topics=${(memoryContext.providedInfo || []).length}`);
       }
 
       // ====================================================================================================
@@ -624,19 +624,19 @@ class EmailProcessor {
       // ====================================================================================================
       const salutationMode = computeSalutationMode({
         isReply: safeSubjectLower.startsWith('re:'),
-        messageCount: memoryContext.messageCount || messages.length, // Il conteggio include gi├á mess.length attuali
+        messageCount: memoryContext.messageCount || messages.length, // Il conteggio include gi├â┬á mess.length attuali
         memoryExists: Object.keys(memoryContext).length > 0,
         lastUpdated: memoryContext.lastUpdated || null,
         now: new Date()
       });
-      console.log(`   ­ƒôè Modalit├á saluto: ${salutationMode}`);
+      console.log(`   ├░┼©ÔÇ£┼á Modalit├â┬á saluto: ${salutationMode}`);
 
       const responseDelay = computeResponseDelay({
         messageDate: messageDetails.date,
         now: new Date()
       });
       if (responseDelay.shouldApologize) {
-        console.log(`   ­ƒòÉ Ritardo risposta: ${responseDelay.days} giorni`);
+        console.log(`   ├░┼©ÔÇó┬É Ritardo risposta: ${responseDelay.days} giorni`);
       }
 
       // ====================================================================================================
@@ -668,11 +668,11 @@ class EmailProcessor {
       const addressLines = territoryResult.addressFound
         ? (territoryResult.addresses || []).map((entry) => {
           const v = entry.verification || {};
-          const sanitizedStreet = (entry.street || '').replace(/[=ÔöÇ]/g, '-');
+          const sanitizedStreet = (entry.street || '').replace(/[=├óÔÇØÔé¼]/g, '-');
           const civicLabel = entry.civic ? `n. ${entry.civic}` : 'senza numero civico';
           const resultLabel = v.needsCivic
-            ? 'ÔÜá´©Å CIVICO NECESSARIO'
-            : (v.inParish ? 'Ô£à RIENTRA' : 'ÔØî NON RIENTRA');
+            ? '├ó┼í┬á├»┬©┬Å CIVICO NECESSARIO'
+            : (v.inParish ? '├ó┼ôÔÇª RIENTRA' : '├ó┬Ø┼Æ NON RIENTRA');
           const actionLabel = v.needsCivic ? 'Azione: richiedere il numero civico.' : null;
           return [
             `Indirizzo: ${sanitizedStreet} ${civicLabel}`,
@@ -685,7 +685,7 @@ class EmailProcessor {
 
       const territoryContext = `
 ====================================================================================================
-­ƒÄ» VERIFICA TERRITORIO AUTOMATICA
+├░┼©┼¢┬» VERIFICA TERRITORIO AUTOMATICA
 ====================================================================================================
 ${addressLines.join('\n\n')}
 ====================================================================================================
@@ -695,7 +695,7 @@ ${addressLines.join('\n\n')}
       const summary = territoryResult.addressFound
         ? (addressLines.length > 1 ? `${addressLines.length} indirizzi` : '1 indirizzo')
         : 'nessun indirizzo';
-      console.log(`   ­ƒÄ» Verifica territorio: ${summary}`);
+      console.log(`   ├░┼©┼¢┬» Verifica territorio: ${summary}`);
 
       // ====================================================================================================
       // STEP 7.2: PROMPT CONTEXT (profilo e concern dinamici)
@@ -732,7 +732,7 @@ ${addressLines.join('\n\n')}
         });
         promptProfile = promptContext.profile;
         activeConcerns = promptContext.concerns;
-        console.log(`   ­ƒºá PromptContext: profilo=${promptProfile}`);
+        console.log(`   ├░┼©┬º┬á PromptContext: profilo=${promptProfile}`);
       }
 
       const categoryHintSource = classification.category || requestType.type;
@@ -742,22 +742,22 @@ ${addressLines.join('\n\n')}
       // ====================================================================================================
       let attachmentContext = { text: '', items: [], skipped: [] };
       if (typeof CONFIG !== 'undefined' && CONFIG.ATTACHMENT_CONTEXT && CONFIG.ATTACHMENT_CONTEXT.enabled) {
-        // OCR pu├▓ essere costoso: se siamo vicini alla deadline, preferiamo degradare e proseguire.
+        // OCR pu├â┬▓ essere costoso: se siamo vicini alla deadline, preferiamo degradare e proseguire.
         if (this._isNearDeadline(this.config.maxExecutionTimeMs)) {
           attachmentContext.skipped.push({ reason: 'near_deadline' });
-          console.warn('   ÔÅ│ OCR allegati saltato: tempo residuo insufficiente.');
+          console.warn('   ├ó┬Å┬│ OCR allegati saltato: tempo residuo insufficiente.');
         } else {
           let hasAttachments = false;
           try {
             const attachments = candidate.getAttachments({ includeInlineImages: true, includeAttachments: true }) || [];
             hasAttachments = attachments.length > 0;
           } catch (e) {
-            console.warn(`ÔÜá´©Å Impossibile leggere allegati per pre-check OCR: ${e.message}`);
+            console.warn(`├ó┼í┬á├»┬©┬Å Impossibile leggere allegati per pre-check OCR: ${e.message}`);
           }
 
           if (!hasAttachments) {
             attachmentContext.skipped.push({ reason: 'no_attachments' });
-            console.log('   ­ƒôÄ OCR saltato: nessun allegato nel messaggio candidato');
+            console.log('   ├░┼©ÔÇ£┼¢ OCR saltato: nessun allegato nel messaggio candidato');
           } else if (this._shouldTryOcr(messageDetails.body, messageDetails.subject, candidate)) {
             attachmentContext = this.gmailService.extractAttachmentContext(candidate, {
               detectedLanguage: detectedLanguage,
@@ -765,7 +765,7 @@ ${addressLines.join('\n\n')}
             });
           } else {
             attachmentContext.skipped.push({ reason: 'precheck_no_ocr' });
-            console.log('   ­ƒôÄ Allegati OCR saltati: pre-check negativo (keyword non trovate)');
+            console.log('   ├░┼©ÔÇ£┼¢ Allegati OCR saltati: pre-check negativo (keyword non trovate)');
           }
         }
       } else {
@@ -773,10 +773,10 @@ ${addressLines.join('\n\n')}
       }
       if (attachmentContext && attachmentContext.items && attachmentContext.items.length > 0) {
         const attachmentNames = attachmentContext.items.map(item => item.name).join(', ');
-        console.log(`   ­ƒôÄ Allegati OCR: ${attachmentContext.items.length} file inclusi nel contesto (${attachmentNames})`);
+        console.log(`   ├░┼©ÔÇ£┼¢ Allegati OCR: ${attachmentContext.items.length} file inclusi nel contesto (${attachmentNames})`);
       } else if (attachmentContext && attachmentContext.skipped && attachmentContext.skipped.length > 0) {
         const skippedNames = attachmentContext.skipped.map(item => item.name || item.reason).join(', ');
-        console.log(`   ­ƒôÄ Allegati OCR saltati: ${attachmentContext.skipped.length} (${skippedNames})`);
+        console.log(`   ├░┼©ÔÇ£┼¢ Allegati OCR saltati: ${attachmentContext.skipped.length} (${skippedNames})`);
       }
 
       const promptOptions = {
@@ -815,13 +815,13 @@ ${addressLines.join('\n\n')}
       // STEP 8: GENERA RISPOSTA (STRATEGIA "CROSS-KEY QUALITY FIRST")
       // ====================================================================================================
       // NOTA ARCHITETTURALE:
-      // Questa fase pu├▓ richiedere pi├╣ tempo del normale (fino a 4 tentativi API).
-      // SCELTA DELIBERATA: Privilegiamo la qualit├á della risposta (Modello Flash 2.5)
-      // rispetto alla velocit├á. 
+      // Questa fase pu├â┬▓ richiedere pi├â┬╣ tempo del normale (fino a 4 tentativi API).
+      // SCELTA DELIBERATA: Privilegiamo la qualit├â┬á della risposta (Modello Flash 2.5)
+      // rispetto alla velocit├â┬á. 
       // 1. Proviamo Flash 2.5 sulla chiave primaria.
       // 2. Se fallisce, proviamo Flash 2.5 sulla chiave di RISERVA.
-      // 3. Solo se entrambe falliscono, degradiamo al modello Lite (pi├╣ economico).
-      // Questo "costo" in termini di tempo ├¿ gestito riducendo MAX_EMAILS_PER_RUN a 3.
+      // 3. Solo se entrambe falliscono, degradiamo al modello Lite (pi├â┬╣ economico).
+      // Questo "costo" in termini di tempo ├â┬¿ gestito riducendo MAX_EMAILS_PER_RUN a 3.
       // ====================================================================================================
 
       let response = null;
@@ -830,7 +830,7 @@ ${addressLines.join('\n\n')}
       let strategyUsed = null;
 
       if (this._isNearDeadline(this.config.maxExecutionTimeMs)) {
-        console.warn('ÔÅ│ Tempo residuo insufficiente prima della generazione AI: rimando il thread al prossimo turno.');
+        console.warn('├ó┬Å┬│ Tempo residuo insufficiente prima della generazione AI: rimando il thread al prossimo turno.');
         result.status = 'skipped';
         result.reason = 'near_deadline_before_generation';
         return result;
@@ -856,7 +856,7 @@ ${addressLines.join('\n\n')}
         if (!plan.key) continue;
 
         try {
-          console.log(`­ƒöä Tentativo Generazione: ${plan.name}...`);
+          console.log(`├░┼©ÔÇØÔÇ× Tentativo Generazione: ${plan.name}...`);
 
           response = this.geminiService.generateResponse(fullPrompt, {
             apiKey: plan.key,
@@ -864,7 +864,7 @@ ${addressLines.join('\n\n')}
             skipRateLimit: plan.skipRateLimit
           });
 
-          // GeminiService pu├▓ restituire stringa semplice oppure
+          // GeminiService pu├â┬▓ restituire stringa semplice oppure
           // oggetto strutturato { success, text, modelUsed }.
           if (response && typeof response === 'object') {
             response = response.text;
@@ -872,7 +872,7 @@ ${addressLines.join('\n\n')}
 
           if (response) {
             strategyUsed = plan.name;
-            console.log(`Ô£à Generazione riuscita con strategia: ${plan.name}`);
+            console.log(`├ó┼ôÔÇª Generazione riuscita con strategia: ${plan.name}`);
             break; // Successo! Esci dal loop
           }
 
@@ -880,15 +880,15 @@ ${addressLines.join('\n\n')}
           generationError = err; // Salva l'ultimo errore
           if (!initialError) initialError = err; // Salva il primo errore per il reporting
           const errorClass = this._classifyError(err);
-          console.warn(`ÔÜá´©Å Strategia '${plan.name}' fallita: ${err.message} [${errorClass.type}]`);
+          console.warn(`├ó┼í┬á├»┬©┬Å Strategia '${plan.name}' fallita: ${err.message} [${errorClass.type}]`);
 
           if (errorClass.type === 'FATAL') {
-            console.error('­ƒøæ Errore fatale rilevato, interrompo strategia.');
+            console.error('├░┼©ÔÇ║ÔÇÿ Errore fatale rilevato, interrompo strategia.');
             break;
           }
 
           if (errorClass.type === 'NETWORK') {
-            console.warn('­ƒîÉ Errore di rete/timeout, continuo con prossima strategia.');
+            console.warn('├░┼©┼Æ┬É Errore di rete/timeout, continuo con prossima strategia.');
             continue;
           }
           // QUOTA_EXCEEDED, INVALID_RESPONSE, UNKNOWN: continua
@@ -899,7 +899,7 @@ ${addressLines.join('\n\n')}
       if (!response) {
         const errorToReport = initialError || generationError;
         const errorClass = errorToReport ? this._classifyError(errorToReport) : { type: 'UNKNOWN', retryable: false, message: 'Generation strategies exhausted' };
-        console.error('­ƒøæ TUTTE le strategie di generazione sono fallite.');
+        console.error('├░┼©ÔÇ║ÔÇÿ TUTTE le strategie di generazione sono fallite.');
         this._addErrorLabel(thread);
         this._markMessageAsProcessed(candidate, labeledMessageIds);
         result.status = 'error';
@@ -913,7 +913,7 @@ ${addressLines.join('\n\n')}
 
       // Controlla marcatore NO_REPLY
       if (typeof response !== 'string') {
-        console.error(`­ƒøæ Risposta non valida da Gemini: tipo ricevuto '${typeof response}'`);
+        console.error(`├░┼©ÔÇ║ÔÇÿ Risposta non valida da Gemini: tipo ricevuto '${typeof response}'`);
         this._addErrorLabel(thread);
         this._markMessageAsProcessed(candidate, labeledMessageIds);
         result.status = 'error';
@@ -923,18 +923,18 @@ ${addressLines.join('\n\n')}
       }
 
       if (response.trim() === 'NO_REPLY') {
-        console.log('   Ôèû AI ha restituito NO_REPLY');
+        console.log('   ├ó┼áÔÇô AI ha restituito NO_REPLY');
         this._markMessageAsProcessed(candidate, labeledMessageIds);
         result.status = 'filtered';
         return result;
       }
 
-      // Nota OCR bassa confidenza: avviso leggibilit├á allegato
+      // Nota OCR bassa confidenza: avviso leggibilit├â┬á allegato
       if (attachmentContext && attachmentContext.ocrConfidenceLow) {
         const ocrLowConfidenceNote = this._getOcrLowConfidenceNote(detectedLanguage);
         if (ocrLowConfidenceNote && !response.includes(ocrLowConfidenceNote)) {
           response = `${response.trim()}\n\n${ocrLowConfidenceNote}`;
-          console.log(`   ÔÜá´©Å Nota OCR aggiunta (confidenza media: ${attachmentContext.ocrConfidence})`);
+          console.log(`   ├ó┼í┬á├»┬©┬Å Nota OCR aggiunta (confidenza media: ${attachmentContext.ocrConfidence})`);
         }
       }
 
@@ -952,11 +952,11 @@ ${addressLines.join('\n\n')}
         );
 
         if (!validation.isValid) {
-          console.warn(`   ­ƒøæ Validazione FALLITA (punteggio: ${validation.score.toFixed(2)})`);
+          console.warn(`   ├░┼©ÔÇ║ÔÇÿ Validazione FALLITA (punteggio: ${validation.score.toFixed(2)})`);
 
           // Gestione errore validazione critica
           if (validation.details && validation.details.exposedReasoning && validation.details.exposedReasoning.score === 0.0) {
-            console.warn("ÔÜá´©Å Risposta bloccata per Thinking Leak. Invio a etichetta 'Verifica'.");
+            console.warn("├ó┼í┬á├»┬©┬Å Risposta bloccata per Thinking Leak. Invio a etichetta 'Verifica'.");
             result.reason = 'thinking_leak';
           }
 
@@ -971,35 +971,35 @@ ${addressLines.join('\n\n')}
           return result;
         }
 
-        // Se ci sono WARNING e il punteggio ├¿ sotto la soglia di sicurezza, aggiungi etichetta "verifica"
+        // Se ci sono WARNING e il punteggio ├â┬¿ sotto la soglia di sicurezza, aggiungi etichetta "verifica"
         // Ignoriamo i warning per punteggi alti (es. >= 0.90) assumendo siano nits minori (es. firma)
         const warningThreshold = this.config.validationWarningThreshold || 0.90;
 
         if (validation.warnings && validation.warnings.length > 0 && validation.score < warningThreshold) {
           console.log(
-            `   ÔÜá´©Å Validazione: Punteggio ${validation.score.toFixed(2)} < ${warningThreshold} con warning - Aggiungo etichetta '${this.config.validationErrorLabel}'`
+            `   ├ó┼í┬á├»┬©┬Å Validazione: Punteggio ${validation.score.toFixed(2)} < ${warningThreshold} con warning - Aggiungo etichetta '${this.config.validationErrorLabel}'`
           );
           this.gmailService.addLabelToMessage(candidate.getId(), this.config.validationErrorLabel);
         } else if (validation.warnings && validation.warnings.length > 0) {
-          console.log(`   Ôä╣´©Å Validazione: Punteggio alto (${validation.score.toFixed(2)}). Warning ignorati: ${validation.warnings.join(', ')}`);
+          console.log(`   ├óÔÇ×┬╣├»┬©┬Å Validazione: Punteggio alto (${validation.score.toFixed(2)}). Warning ignorati: ${validation.warnings.join(', ')}`);
         }
 
         if (validation.fixedResponse) {
-          console.log('   ­ƒ®╣ Usa risposta corretta automaticamente (Self-Healing)');
+          console.log('   ├░┼©┬®┬╣ Usa risposta corretta automaticamente (Self-Healing)');
           response = validation.fixedResponse;
         }
 
-        console.log(`   Ô£ô Validazione PASSATA (punteggio: ${validation.score.toFixed(2)})`);
+        console.log(`   ├ó┼ôÔÇ£ Validazione PASSATA (punteggio: ${validation.score.toFixed(2)})`);
       }
 
       // ====================================================================================================
       // STEP 10: INVIA RISPOSTA
       // ====================================================================================================
       if (this.config.dryRun) {
-        console.log('   ­ƒö┤ DRY RUN - Risposta non inviata');
-        console.log(`   ­ƒôä Invierebbe: ${response.substring(0, 100)}...`);
+        console.log('   ├░┼©ÔÇØ┬┤ DRY RUN - Risposta non inviata');
+        console.log(`   ├░┼©ÔÇ£ÔÇ× Invierebbe: ${response.substring(0, 100)}...`);
         result.dryRun = true;
-        // In DRY_RUN non aggiorniamo memoria n├® label per non avere effetti permanenti
+        // In DRY_RUN non aggiorniamo memoria n├â┬® label per non avere effetti permanenti
         result.status = 'replied';
         result.durationMs = Date.now() - startTime;
         this.logger.info(`Thread processato in ${result.durationMs}ms`, { threadId: threadId, duration: result.durationMs });
@@ -1010,7 +1010,7 @@ ${addressLines.join('\n\n')}
         this.gmailService.sendHtmlReply(candidate, response, messageDetails);
       } catch (e) {
         const errorMessage = e && e.message ? e.message : String(e);
-        console.error(`   ­ƒøæ Errore invio Gmail: ${errorMessage}`);
+        console.error(`   ├░┼©ÔÇ║ÔÇÿ Errore invio Gmail: ${errorMessage}`);
         this._addErrorLabel(thread);
         if (candidate) {
           this._markMessageAsProcessed(candidate, labeledMessageIds);
@@ -1064,7 +1064,7 @@ ${addressLines.join('\n\n')}
       return result;
 
     } catch (error) {
-      console.error(`   ­ƒøæ Errore elaborazione thread: ${error.message}`);
+      console.error(`   ├░┼©ÔÇ║ÔÇÿ Errore elaborazione thread: ${error.message}`);
       this._addErrorLabel(thread);
       if (candidate) {
         this._markMessageAsProcessed(candidate, labeledMessageIds);
@@ -1080,17 +1080,17 @@ ${addressLines.join('\n\n')}
       if (lockAcquired && scriptCache && threadLockKey) {
         try {
           const currentLockValue = scriptCache.get(threadLockKey);
-          // Rilasciamo sia quando il lock ├¿ nostro sia quando ├¿ gi├á scaduto.
+          // Rilasciamo sia quando il lock ├â┬¿ nostro sia quando ├â┬¿ gi├â┬á scaduto.
           if (!currentLockValue || currentLockValue === lockValue) {
             scriptCache.remove(threadLockKey);
-            console.log(`­ƒöô Lock rilasciato per thread ${threadId}`);
+            console.log(`├░┼©ÔÇØÔÇ£ Lock rilasciato per thread ${threadId}`);
           } else {
-            console.warn(`ÔÜá´©Å Rilascio lock saltato per thread ${threadId} (lock scaduto o di altro processo)`);
+            console.warn(`├ó┼í┬á├»┬©┬Å Rilascio lock saltato per thread ${threadId} (lock scaduto o di altro processo)`);
           }
         } catch (e) {
           // Non forziamo remove() se get() fallisce: potremmo cancellare il lock
           // legittimo di un altro worker appena subentrato dopo la scadenza TTL.
-          console.warn('ÔÜá´©Å Errore rilascio lock:', e.message);
+          console.warn('├ó┼í┬á├»┬©┬Å Errore rilascio lock:', e.message);
         }
       }
     }
@@ -1104,13 +1104,13 @@ ${addressLines.join('\n\n')}
    */
   processUnreadEmails(knowledgeBase, doctrineBase = '', skipExecutionLock = true) {
     console.log('\n' + '='.repeat(70));
-    console.log('­ƒô¼ Inizio elaborazione email...');
+    console.log('├░┼©ÔÇ£┬¼ Inizio elaborazione email...');
     console.log('='.repeat(70));
 
     const executionLock = LockService.getScriptLock();
     let lockAcquiredHere = false;
 
-    // Nota: non rimuoviamo questo blocco. Serve quando processUnreadEmails ├¿ invocata
+    // Nota: non rimuoviamo questo blocco. Serve quando processUnreadEmails ├â┬¿ invocata
     // fuori dall'entry point principale (job manuali/test), evitando corse concorrenti.
     // In main il parametro skipExecutionLock=true impedisce il double-lock/release prematuro.
     if (!skipExecutionLock) {
@@ -1118,7 +1118,7 @@ ${addressLines.join('\n\n')}
         ? CONFIG.EXECUTION_LOCK_WAIT_MS : 5000;
 
       if (!executionLock.tryLock(lockWaitMs)) {
-        console.warn('ÔÜá´©Å Un\'altra esecuzione ├¿ gi├á attiva: salto questo turno per evitare doppie risposte.');
+        console.warn('├ó┼í┬á├»┬©┬Å Un\'altra esecuzione ├â┬¿ gi├â┬á attiva: salto questo turno per evitare doppie risposte.');
         return { total: 0, replied: 0, filtered: 0, errors: 0, skipped: 1, reason: 'execution_locked' };
       }
       lockAcquiredHere = true;
@@ -1131,18 +1131,17 @@ ${addressLines.join('\n\n')}
       }
 
       if (this.config.dryRun) {
-        console.warn('­ƒö┤ MODALIT├Ç DRY_RUN ATTIVA - Email NON inviate!');
+        console.warn('├░┼©ÔÇØ┬┤ MODALIT├âÔé¼ DRY_RUN ATTIVA - Email NON inviate!');
       }
 
       // Cerca thread non letti nella inbox
-      // Utilizziamo un buffer di ricerca pi├╣ ampio per gestire thread saltati (es. loop interni)
-      // Rimuoviamo il filtro etichetta per permettere la gestione dei follow-up in thread gi├á elaborati
-      const processedLabelQuery = this._formatLabelQueryValue(this.config.labelName);
+      // Utilizziamo un buffer di ricerca pi├â┬╣ ampio per gestire thread saltati (es. loop interni)
+      // Manteniamo i thread gia processati nella search per consentire follow-up nello stesso thread.
       const errorLabelQuery = this._formatLabelQueryValue(this.config.errorLabelName);
       const validationLabelQuery = this._formatLabelQueryValue(this.config.validationErrorLabel);
 
-      // La query esclude i thread gi├á etichettati (il prefisso "-" ├¿ obbligatorio)
-      const searchQuery = `is:unread -label:${processedLabelQuery} -label:${errorLabelQuery} -label:${validationLabelQuery} in:inbox`;
+      // Escludiamo solo thread gia in errore/verifica; i thread con label IA restano candidati.
+      const searchQuery = `is:unread -label:${errorLabelQuery} -label:${validationLabelQuery} in:inbox`;
       const searchLimit = (this.config.searchPageSize || 50);
 
       const threads = GmailApp.search(
@@ -1156,18 +1155,18 @@ ${addressLines.join('\n\n')}
         console.log(`Nessuna email da elaborare (query: ${searchQuery}).`);
 
         if (emptyStreak >= this.config.emptyInboxWarningThreshold) {
-          console.warn(`ÔÜá´©Å Inbox vuota da ${emptyStreak} esecuzioni consecutive. Verificare filtri Gmail/trigger in ingresso.`);
+          console.warn(`├ó┼í┬á├»┬©┬Å Inbox vuota da ${emptyStreak} esecuzioni consecutive. Verificare filtri Gmail/trigger in ingresso.`);
         }
 
         return { total: 0, replied: 0, filtered: 0, errors: 0, emptyStreak: emptyStreak };
       }
 
       this._trackEmptyInboxStreak(false);
-      console.log(`­ƒô¼ Trovate ${threads.length} email da elaborare (query: ${searchQuery})`);
+      console.log(`├░┼©ÔÇ£┬¼ Trovate ${threads.length} email da elaborare (query: ${searchQuery})`);
 
       // Carica etichette una sola volta
       const labeledMessageIds = this.gmailService.getMessageIdsWithLabel(this.config.labelName);
-      console.log(`­ƒôª Trovati in cache ${labeledMessageIds.size} messaggi gi├á elaborati`);
+      console.log(`├░┼©ÔÇ£┬ª Trovati in cache ${labeledMessageIds.size} messaggi gi├â┬á elaborati`);
 
       // Statistiche
       const stats = {
@@ -1194,7 +1193,7 @@ ${addressLines.join('\n\n')}
       for (let index = 0; index < threads.length; index++) {
         // Stop se abbiamo raggiunto il target di elaborazione effettiva
         if (processedCount >= safeLimit) {
-          console.log(`­ƒøæ Raggiunti ${safeLimit} thread elaborati. Stop.`);
+          console.log(`├░┼©ÔÇ║ÔÇÿ Raggiunti ${safeLimit} thread elaborati. Stop.`);
           break;
         }
 
@@ -1203,15 +1202,15 @@ ${addressLines.join('\n\n')}
         // Controllo tempo residuo (rigoroso): evita avvio di un nuovo thread senza buffer minimo
         const remainingTimeMs = this._getRemainingTimeMs(MAX_EXECUTION_TIME);
         if (remainingTimeMs < this.config.minRemainingTimeMs || this._isNearDeadline(MAX_EXECUTION_TIME)) {
-          console.warn(`ÔÅ│ Tempo insufficiente per un nuovo thread (${Math.round(remainingTimeMs / 1000)}s restanti). Stop preventivo.`);
+          console.warn(`├ó┬Å┬│ Tempo insufficiente per un nuovo thread (${Math.round(remainingTimeMs / 1000)}s restanti). Stop preventivo.`);
           break;
         }
 
-        // Fast-skip: evita pipeline completa/lock quando i non letti sono gi├á etichettati.
-        // Riduce overhead su thread marcati manualmente come "non letto" ma gi├á gestiti.
+        // Fast-skip: evita pipeline completa/lock quando i non letti sono gi├â┬á etichettati.
+        // Riduce overhead su thread marcati manualmente come "non letto" ma gi├â┬á gestiti.
         if (!this._hasUnreadMessagesToProcess(thread, labeledMessageIds)) {
           console.log(`\n--- Thread ${index + 1}/${threads.length} ---`);
-          console.log('   Ôèû Fast-skip: thread con soli non letti gi├á etichettati IA');
+          console.log('   ├ó┼áÔÇô Fast-skip: thread con soli non letti gi├â┬á etichettati IA');
           stats.total++;
           stats.skipped++;
           stats.skipped_processed++;
@@ -1223,7 +1222,7 @@ ${addressLines.join('\n\n')}
         const result = this.processThread(thread, knowledgeBase, doctrineBase, labeledMessageIds);
         stats.total++;
 
-        // Incrementa contatore solo se c'├¿ stata un'azione significativa o decisione esplicita dell'AI
+        // Incrementa contatore solo se c'├â┬¿ stata un'azione significativa o decisione esplicita dell'AI
         const isEffectiveWork = (
           result.status === 'replied' ||
           result.status === 'error' ||
@@ -1256,19 +1255,19 @@ ${addressLines.join('\n\n')}
 
       // Stampa riepilogo
       console.log('\n' + '='.repeat(70));
-      console.log('­ƒôè RIEPILOGO ELABORAZIONE');
+      console.log('├░┼©ÔÇ£┼á RIEPILOGO ELABORAZIONE');
       console.log('='.repeat(70));
       console.log(`   Totale analizzate (buffer): ${stats.total}`);
-      console.log(`   Ô£ô Risposte inviate: ${stats.replied}`);
-      if (stats.dryRun > 0) console.warn(`   ­ƒö┤ DRY RUN: ${stats.dryRun}`);
+      console.log(`   ├ó┼ôÔÇ£ Risposte inviate: ${stats.replied}`);
+      if (stats.dryRun > 0) console.warn(`   ├░┼©ÔÇØ┬┤ DRY RUN: ${stats.dryRun}`);
 
       if (stats.skipped > 0) {
-        console.log(`   Ôèû Saltate (Totale): ${stats.skipped}`);
+        console.log(`   ├ó┼áÔÇô Saltate (Totale): ${stats.skipped}`);
       }
 
-      console.log(`   Ôèû Filtrate (AI/Regole): ${stats.filtered}`);
-      if (stats.validationFailed > 0) console.warn(`   ­ƒøæ Validazione fallita: ${stats.validationFailed}`);
-      if (stats.errors > 0) console.error(`   ­ƒøæ Errori: ${stats.errors}`);
+      console.log(`   ├ó┼áÔÇô Filtrate (AI/Regole): ${stats.filtered}`);
+      if (stats.validationFailed > 0) console.warn(`   ├░┼©ÔÇ║ÔÇÿ Validazione fallita: ${stats.validationFailed}`);
+      if (stats.errors > 0) console.error(`   ├░┼©ÔÇ║ÔÇÿ Errori: ${stats.errors}`);
       stats.processed = processedCount;
       console.log('='.repeat(70));
 
@@ -1279,7 +1278,7 @@ ${addressLines.join('\n\n')}
         try {
           executionLock.releaseLock();
         } catch (e) {
-          console.warn(`ÔÜá´©Å Errore rilascio execution lock: ${e.message}`);
+          console.warn(`├ó┼í┬á├»┬©┬Å Errore rilascio execution lock: ${e.message}`);
         }
       }
     }
@@ -1299,7 +1298,7 @@ ${addressLines.join('\n\n')}
     const body = (messageDetails.body || '').toLowerCase();
 
     // 1. Controllo Blacklist Domini/Email
-    // NOTA: GLOBAL_CACHE.ignoreDomains include gi├á CONFIG.IGNORE_DOMAINS (merge in _loadAdvancedConfig)
+    // NOTA: GLOBAL_CACHE.ignoreDomains include gi├â┬á CONFIG.IGNORE_DOMAINS (merge in _loadAdvancedConfig)
     const ignoreDomains = (typeof GLOBAL_CACHE !== 'undefined' && Array.isArray(GLOBAL_CACHE.ignoreDomains))
       ? GLOBAL_CACHE.ignoreDomains.map(d => String(d).toLowerCase())
       : ((typeof CONFIG !== 'undefined' && CONFIG.IGNORE_DOMAINS) ? CONFIG.IGNORE_DOMAINS.map(d => String(d).toLowerCase()) : []);
@@ -1309,18 +1308,18 @@ ${addressLines.join('\n\n')}
       const isDomainMatch = email.endsWith(domain.startsWith('@') ? domain : '@' + domain);
       return isExactMatch || isDomainMatch;
     })) {
-      console.log(`­ƒÜ½ Ignorato: mittente in blacklist (${email})`);
+      console.log(`├░┼©┼í┬½ Ignorato: mittente in blacklist (${email})`);
       return true;
     }
 
     // 2. Controllo Keyword Oggetto/Corpo
-    // NOTA: GLOBAL_CACHE.ignoreKeywords include gi├á CONFIG.IGNORE_KEYWORDS (merge in _loadAdvancedConfig)
+    // NOTA: GLOBAL_CACHE.ignoreKeywords include gi├â┬á CONFIG.IGNORE_KEYWORDS (merge in _loadAdvancedConfig)
     const ignoreKeywords = (typeof GLOBAL_CACHE !== 'undefined' && Array.isArray(GLOBAL_CACHE.ignoreKeywords))
       ? GLOBAL_CACHE.ignoreKeywords.map(k => String(k).toLowerCase())
       : ((typeof CONFIG !== 'undefined' && CONFIG.IGNORE_KEYWORDS) ? CONFIG.IGNORE_KEYWORDS.map(k => String(k).toLowerCase()) : []);
 
     if (ignoreKeywords.some(keyword => subject.includes(keyword) || body.includes(keyword))) {
-      console.log(`­ƒÜ½ Ignorato: oggetto o corpo contiene keyword vietata`);
+      console.log(`├░┼©┼í┬½ Ignorato: oggetto o corpo contiene keyword vietata`);
       return true;
     }
 
@@ -1340,7 +1339,7 @@ ${addressLines.join('\n\n')}
       body.includes('this is an automatically generated message') ||
       body.includes('do not reply to this email')
     ) {
-      console.log('­ƒÜ½ Ignorato: auto-reply o notifica di sistema');
+      console.log('├░┼©┼í┬½ Ignorato: auto-reply o notifica di sistema');
       return true;
     }
 
@@ -1356,7 +1355,7 @@ ${addressLines.join('\n\n')}
     // Se presenti in configurazione, verranno usate come filtro.
     const triggerKeywords = settings.ocrTriggerKeywords || [];
 
-    // Se la lista ├¿ vuota, significa "OCR sempre attivo se enabled=true"
+    // Se la lista ├â┬¿ vuota, significa "OCR sempre attivo se enabled=true"
     if (triggerKeywords.length === 0) return true;
 
     const normalizedBody = (body || '').toLowerCase().replace(/\s+/g, ' ');
@@ -1376,11 +1375,11 @@ ${addressLines.join('\n\n')}
       try {
         const attachments = message.getAttachments({ includeInlineImages: true, includeAttachments: true }) || [];
         if (attachments.length > 0) {
-          console.log('   ­ƒôÄ OCR fallback attivo: email senza testo ma con allegati');
+          console.log('   ├░┼©ÔÇ£┼¢ OCR fallback attivo: email senza testo ma con allegati');
           return true;
         }
       } catch (e) {
-        console.warn(`ÔÜá´©Å Impossibile verificare allegati per OCR fallback: ${e.message}`);
+        console.warn(`├ó┼í┬á├»┬©┬Å Impossibile verificare allegati per OCR fallback: ${e.message}`);
       }
     }
 
@@ -1397,9 +1396,9 @@ ${addressLines.join('\n\n')}
     const notes = {
       it: 'Nota: Il documento allegato era di difficile lettura.',
       en: 'Note: The attached document was difficult to read.',
-      es: 'Nota: El documento adjunto era dif├¡cil de leer.',
-      fr: 'Remarque : Le document joint ├®tait difficile ├á lire.',
-      de: 'Hinweis: Das angeh├ñngte Dokument war schwer lesbar.',
+      es: 'Nota: El documento adjunto era dif├â┬¡cil de leer.',
+      fr: 'Remarque : Le document joint ├â┬®tait difficile ├â┬á lire.',
+      de: 'Hinweis: Das angeh├â┬ñngte Dokument war schwer lesbar.',
       pt: 'Nota: O documento em anexo estava difficile de ler. Posso ter omitido alguns detalhes.'
     };
     return notes[lang] || notes.it;
@@ -1409,7 +1408,7 @@ ${addressLines.join('\n\n')}
     // SCELTA OPERATIVA INTENZIONALE:
     // - etichetta IA a livello *messaggio* (non thread), usando Gmail Advanced Service;
     // - NON marcare come letto qui.
-    // Motivo: il segretario deve vedere a colpo d'occhio i non letti, anche se gi├á gestiti da IA.
+    // Motivo: il segretario deve vedere a colpo d'occhio i non letti, anche se gi├â┬á gestiti da IA.
     // Cambiare questo comportamento altera la triage operativa.
     const messageId = message.getId();
     this.gmailService.addLabelToMessage(messageId, this.config.labelName);
@@ -1418,7 +1417,7 @@ ${addressLines.join('\n\n')}
     }
   }
 
-  // Calcola se il tempo residuo ├¿ sufficiente per elaborare un nuovo thread
+  // Calcola se il tempo residuo ├â┬¿ sufficiente per elaborare un nuovo thread
   _isNearDeadline(maxExecutionTimeMs) {
     const budgetMs = Number(maxExecutionTimeMs) || 330000;
     const minRemainingMs = (typeof this.config.minRemainingTimeMs === 'number')
@@ -1441,8 +1440,8 @@ ${addressLines.join('\n\n')}
   _formatLabelQueryValue(labelName) {
     const raw = String(labelName || '').trim();
     if (!raw) return '""';
-    // Scelta intenzionale: in query Gmail il quoting ├¿ la strategia pi├╣ stabile.
-    // Evitiamo escaping aggressivo di caratteri non-quote perch├® pu├▓ alterare il matching label:nome.
+    // Scelta intenzionale: in query Gmail il quoting ├â┬¿ la strategia pi├â┬╣ stabile.
+    // Evitiamo escaping aggressivo di caratteri non-quote perch├â┬® pu├â┬▓ alterare il matching label:nome.
     return `"${raw.replace(/"/g, '\\"')}"`;
   }
 
@@ -1451,7 +1450,7 @@ ${addressLines.join('\n\n')}
       const messages = thread.getMessages() || [];
       const unreadMessages = messages.filter(m => m.isUnread());
 
-      // Nessun non letto: non c'├¿ lavoro da fare.
+      // Nessun non letto: non c'├â┬¿ lavoro da fare.
       if (unreadMessages.length === 0) {
         return false;
       }
@@ -1463,7 +1462,7 @@ ${addressLines.join('\n\n')}
       return unreadMessages.some(message => !effectiveLabeledIds.has(message.getId()));
     } catch (e) {
       // Fallback sicuro: in caso di errore non bloccare il thread, lasciamo decidere a processThread.
-      this.logger.warn(`ÔÜá´©Å Fast-skip check fallito: ${e.message}`);
+      this.logger.warn(`├ó┼í┬á├»┬©┬Å Fast-skip check fallito: ${e.message}`);
       return true;
     }
   }
@@ -1525,9 +1524,9 @@ ${addressLines.join('\n\n')}
     }
 
     // Aggiungiamo data contestuale per non collassare topic se esauditi in momenti diversi
-    const newBullet = summarySentence ? `ÔÇó [${new Date().toISOString().split('T')[0]}] ${summarySentence}` : '';
+    const newBullet = summarySentence ? `├óÔé¼┬ó [${new Date().toISOString().split('T')[0]}] ${summarySentence}` : '';
 
-    // Controlliamo l'overlap ma con tolleranza (data aiuta l'unicit├á)
+    // Controlliamo l'overlap ma con tolleranza (data aiuta l'unicit├â┬á)
     if (newBullet && !summaryLines.some(line => line.toLowerCase() === newBullet.toLowerCase())) {
       summaryLines.push(newBullet);
     }
@@ -1552,7 +1551,7 @@ ${addressLines.join('\n\n')}
   _detectProvidedTopics(response) {
     if (!response || typeof response !== 'string') return [];
     const topics = [];
-    // I pattern usano gi├á il flag /i, nessuna normalizzazione necessaria
+    // I pattern usano gi├â┬á il flag /i, nessuna normalizzazione necessaria
 
     const patterns = {
       'orari_messe': /messe?\b.*\d{1,2}[:.]\d{2}|orari\w*\s+messe/i,
@@ -1562,7 +1561,7 @@ ${addressLines.join('\n\n')}
       'cresima_info': /cresima.*percorso|percorso.*cresima/i,
       'matrimonio_info': /matrimonio.*corso|corso.*matrimonio/i,
       'territorio': /rientra|non rientra|parrocchia.*competenza/i,
-      'indirizzo': /(?:via|viale|corso|piazza|largo|circonvallazione)\s+(?:[a-zA-Z├á├¿├®├¼├▓├╣├Ç├ê├ë├î├Æ├Ö']+\s+){0,10}[a-zA-Z├á├¿├®├¼├▓├╣├Ç├ê├ë├î├Æ├Ö']+\s*,?\s*\d+/i
+      'indirizzo': /(?:via|viale|corso|piazza|largo|circonvallazione)\s+(?:[a-zA-Z├â┬á├â┬¿├â┬®├â┬¼├â┬▓├â┬╣├âÔé¼├â╦å├âÔÇ░├â┼Æ├âÔÇÖ├âÔäó']+\s+){0,10}[a-zA-Z├â┬á├â┬¿├â┬®├â┬¼├â┬▓├â┬╣├âÔé¼├â╦å├âÔÇ░├â┼Æ├âÔÇÖ├âÔäó']+\s*,?\s*\d+/i
     };
 
     for (const [topic, pattern] of Object.entries(patterns)) {
@@ -1585,30 +1584,30 @@ ${addressLines.join('\n\n')}
     // Pattern semplici di reazione
     const patterns = {
       questioned: [
-        'non ho capito', 'non capisco', 'mi scusi non ho capito', 'non mi ├¿ chiaro',
-        'non ├¿ chiaro', 'pu├▓ chiarire', 'potrebbe chiarire', 'potrebbe spiegare',
+        'non ho capito', 'non capisco', 'mi scusi non ho capito', 'non mi ├â┬¿ chiaro',
+        'non ├â┬¿ chiaro', 'pu├â┬▓ chiarire', 'potrebbe chiarire', 'potrebbe spiegare',
         'cosa significa', 'dubbio', 'confuso', 'mi aiuta a capire',
         'i did not understand', 'i don\'t understand', 'not clear',
         'could you clarify', 'could you please clarify', 'could you explain',
-        'no entiendo', 'no entend├¡', 'no me queda claro', 'podr├¡a aclarar',
-        'podr├¡a explicar', 'podr├¡a ayudarme a entender'
+        'no entiendo', 'no entend├â┬¡', 'no me queda claro', 'podr├â┬¡a aclarar',
+        'podr├â┬¡a explicar', 'podr├â┬¡a ayudarme a entender'
       ],
       acknowledged: [
         'ho capito', 'tutto chiaro', 'grazie per la spiegazione', 'ok grazie',
         'perfetto', 'chiarissimo', 'ricevuto', 'la ringrazio', 'grazie',
         'gentilissimi', 'va benissimo', 'compreso',
         'thank you', 'thanks', 'understood', 'all clear', 'received',
-        'gracias', 'entendido', 'entendida', 'recibido', 'recibida', 'perfecto', 'clar├¡simo'
+        'gracias', 'entendido', 'entendida', 'recibido', 'recibida', 'perfecto', 'clar├â┬¡simo'
       ],
       needs_expansion: [
         'potrebbe aggiungere', 'potrebbe fornire maggiori dettagli', 'maggiori dettagli',
-        'pi├╣ dettagli', 'approfondire', 'potrebbe spiegare meglio', 'potrebbe ampliare',
-        'sarebbe possibile avere pi├╣ informazioni', 'servirebbero pi├╣ informazioni',
+        'pi├â┬╣ dettagli', 'approfondire', 'potrebbe spiegare meglio', 'potrebbe ampliare',
+        'sarebbe possibile avere pi├â┬╣ informazioni', 'servirebbero pi├â┬╣ informazioni',
         'potrebbe indicare i passaggi',
         'could you provide more details', 'more details', 'could you elaborate',
         'would it be possible to have more information', 'could you outline the steps',
-        'podr├¡a ampliar', 'm├ís detalles', 'podr├¡a proporcionar m├ís informaci├│n',
-        'ser├¡a posible tener m├ís informaci├│n', 'podr├¡a indicar los pasos'
+        'podr├â┬¡a ampliar', 'm├â┬ís detalles', 'podr├â┬¡a proporcionar m├â┬ís informaci├â┬│n',
+        'ser├â┬¡a posible tener m├â┬ís informaci├â┬│n', 'podr├â┬¡a indicar los pasos'
       ]
     };
 
@@ -1652,19 +1651,19 @@ ${addressLines.join('\n\n')}
     };
 
     targetTopics.forEach(topic => {
-      console.log(`­ƒºá Inferred Reaction: ${inferredReaction.type.toUpperCase()} su topic '${topic}'`);
+      console.log(`├░┼©┬º┬á Inferred Reaction: ${inferredReaction.type.toUpperCase()} su topic '${topic}'`);
       this.memoryService.updateReaction(threadId, topic, inferredReaction.type, context);
     });
   }
   /**
    * Punto 12: Classificazione centralizzata degli errori API
-   * Determina se un errore ├¿ fatale, legato alla quota o alla rete.
+   * Determina se un errore ├â┬¿ fatale, legato alla quota o alla rete.
    */
   _classifyError(error) {
     const mkResult = (type, retryable, message) => ({ type, retryable, message });
 
     if (!error) {
-      console.warn('ÔÜá´©Å _classifyError chiamato con errore nullo');
+      console.warn('├ó┼í┬á├»┬©┬Å _classifyError chiamato con errore nullo');
       return mkResult('UNKNOWN', false, '');
     }
 
@@ -1685,10 +1684,10 @@ ${addressLines.join('\n\n')}
       }
     }
 
-    // Classificazione locale (standalone) ÔÇö stesso contratto { type, retryable, message }
+    // Classificazione locale (standalone) ├óÔé¼ÔÇØ stesso contratto { type, retryable, message }
     const msg = String(error.message || error.toString() || '');
     if (!msg) {
-      console.warn('ÔÜá´©Å Errore senza messaggio utile:', error);
+      console.warn('├ó┼í┬á├»┬©┬Å Errore senza messaggio utile:', error);
       return mkResult('UNKNOWN', false, '');
     }
 
@@ -1745,29 +1744,29 @@ ${addressLines.join('\n\n')}
 }
 
 // ====================================================================================================
-// CALCOLATORE MODALIT├Ç SALUTO
+// CALCOLATORE MODALIT├âÔé¼ SALUTO
 // ====================================================================================================
 
 /**
- * Calcola modalit├á saluto basata su segnali strutturali
+ * Calcola modalit├â┬á saluto basata su segnali strutturali
  * @param {Object} params - Parametri di input
  * @returns {'full'|'soft'|'none_or_continuity'|'session'}
  */
 function computeSalutationMode({ isReply = false, messageCount = 0, memoryExists = false, lastUpdated = null, now = new Date() } = {}) {
   const SESSION_WINDOW_MINUTES = 15;
-  // 0´©ÅÔâú Nuovo contatto (non reply): privilegia sempre un saluto completo.
+  // 0├»┬©┬Å├óãÆ┬ú Nuovo contatto (non reply): privilegia sempre un saluto completo.
   // Anche in presenza di memoria pregressa, un nuovo thread/messaggio iniziale
-  // deve evitare modalit├á "none_or_continuity".
+  // deve evitare modalit├â┬á "none_or_continuity".
   if (!isReply) {
     return 'full';
   }
 
-  // 1´©ÅÔâú Primo messaggio assoluto
+  // 1├»┬©┬Å├óãÆ┬ú Primo messaggio assoluto
   if (!memoryExists && messageCount <= 1) {
     return 'full';
   }
 
-  // 2´©ÅÔâú Conversazione attiva (qui isReply ├¿ necessariamente true)
+  // 2├»┬©┬Å├óãÆ┬ú Conversazione attiva (qui isReply ├â┬¿ necessariamente true)
   if (!lastUpdated) {
     return 'none_or_continuity';
   }
@@ -1782,7 +1781,7 @@ function computeSalutationMode({ isReply = false, messageCount = 0, memoryExists
   const hoursSinceLast = timeSinceLastMs / (1000 * 60 * 60);
 
   if (isNaN(hoursSinceLast) || hoursSinceLast < 0) {
-    console.warn('ÔÜá´©Å Timestamp futuro o invalido');
+    console.warn('├ó┼í┬á├»┬©┬Å Timestamp futuro o invalido');
     return 'full';
   }
 
@@ -1801,11 +1800,11 @@ function computeSalutationMode({ isReply = false, messageCount = 0, memoryExists
     return 'soft';
   }
 
-  // Troppo tempo passato (> 4 giorni) ÔåÆ nuovo contatto
+  // Troppo tempo passato (> 4 giorni) ├óÔÇáÔÇÖ nuovo contatto
   return 'full';
 }
 
-// Compatibilit├á: rende la funzione disponibile anche in runtime che usano moduli/isolamento
+// Compatibilit├â┬á: rende la funzione disponibile anche in runtime che usano moduli/isolamento
 if (typeof globalThis !== 'undefined' && typeof globalThis.computeSalutationMode !== 'function') {
   globalThis.computeSalutationMode = computeSalutationMode;
 }
@@ -1848,12 +1847,12 @@ function computeResponseDelay({ messageDate, now = new Date(), thresholdHours =
 
 /**
  * Alias dell'entry point principale processEmailsMain() (gas_main.js).
- * Mantenuta per compatibilit├á con trigger preesistenti.
+ * Mantenuta per compatibilit├â┬á con trigger preesistenti.
  */
 function processUnreadEmailsMain() {
   if (typeof processEmailsMain === 'function') {
     processEmailsMain();
   } else {
-    console.error('­ƒøæ processEmailsMain non trovata ÔÇö impossibile delegare.');
+    console.error('├░┼©ÔÇ║ÔÇÿ processEmailsMain non trovata ├óÔé¼ÔÇØ impossibile delegare.');
   }
 }
diff --git a/gas_main.js b/gas_main.js
index 8e418b9..51a7df5 100644
--- a/gas_main.js
+++ b/gas_main.js
@@ -1,7 +1,7 @@
 /**
  * Main.js - Entry point del sistema autoresponder
  * Gestisce trigger, inizializzazione e orchestrazione principale
- * Include logica sospensione oraria e festivit├á italiane
+ * Include logica sospensione oraria e festivit├â┬á italiane
  */
 
 // Inizializzazione difensiva cache globale condivisa tra moduli
@@ -18,8 +18,8 @@ var GLOBAL_CACHE = (typeof GLOBAL_CACHE !== 'undefined' && GLOBAL_CACHE) ? GLOBA
   replacements: {}
 };
 
-// TTL in-RAM valido nella singola esecuzione: cross-esecuzione la cache reale ├¿ CacheService.
-// ÔÜá´©Å Scelta blindata: questo TTL ├¿ allineato ai 21600s della ScriptCache.
+// TTL in-RAM valido nella singola esecuzione: cross-esecuzione la cache reale ├â┬¿ CacheService.
+// ├ó┼í┬á├»┬©┬Å Scelta blindata: questo TTL ├â┬¿ allineato ai 21600s della ScriptCache.
 // Cambiare solo insieme ai comandi manuali di riallineamento (primeCache/clearKnowledgeCache)
 // per evitare disallineamenti "RAM fresca / ScriptCache scaduta" e ricariche imprevedibili.
 const RESOURCE_CACHE_TTL_MS = 6 * 60 * 60 * 1000; // 6 ore
@@ -31,7 +31,7 @@ const RESOURCE_CACHE_PART_PREFIX = `${RESOURCE_CACHE_KEY_V2}:part:`;
 const RESOURCE_CACHE_MAX_PART_SIZE = 95000;
 
 // ====================================================================
-// FESTIVIT├Ç E SOSPENSIONE
+// FESTIVIT├âÔé¼ E SOSPENSIONE
 // ====================================================================
 
 // Costanti per mesi (JavaScript usa indici 0-11)
@@ -56,17 +56,17 @@ const ALWAYS_OPERATING_DAYS = [
 ];
 
 // ====================================================================
-// ÔÜá´©Å WARNING: NON CANCELLARE QUESTO BLOCCO STATIC (Fallback Sicurezza)
+// ├ó┼í┬á├»┬©┬Å WARNING: NON CANCELLARE QUESTO BLOCCO STATIC (Fallback Sicurezza)
 // Orari di sospensione per giorno della settimana
 // Sistema SOSPESO durante questi orari. 
-// Usato come FALLBACK SICURO se il Foglio "Controllo" ├¿ irraggiungibile o formattato male.
-// Qualsiasi sviluppatore intenda rimuoverlo causer├á malfunzionamenti fuori orario in caso di failure API.
+// Usato come FALLBACK SICURO se il Foglio "Controllo" ├â┬¿ irraggiungibile o formattato male.
+// Qualsiasi sviluppatore intenda rimuoverlo causer├â┬á malfunzionamenti fuori orario in caso di failure API.
 const SUSPENSION_HOURS = {
-  1: [[8, 20]],    // Luned├¼: 8ÔÇô20
-  2: [[8, 14]],    // Marted├¼: 8ÔÇô14
-  3: [[8, 17]],    // Mercoled├¼: 8ÔÇô17
-  4: [[8, 14]],    // Gioved├¼: 8ÔÇô14
-  5: [[8, 17]]     // Venerd├¼: 8ÔÇô17
+  1: [[8, 20]],    // Luned├â┬¼: 8├óÔé¼ÔÇ£20
+  2: [[8, 14]],    // Marted├â┬¼: 8├óÔé¼ÔÇ£14
+  3: [[8, 17]],    // Mercoled├â┬¼: 8├óÔé¼ÔÇ£17
+  4: [[8, 14]],    // Gioved├â┬¼: 8├óÔé¼ÔÇ£14
+  5: [[8, 17]]     // Venerd├â┬¼: 8├óÔé¼ÔÇ£17
 };
 
 /**
@@ -96,7 +96,7 @@ function calculateEaster(year) {
  */
 function isInVacationPeriod(date = new Date(), scriptTimeZone = "") {
   if (!(date instanceof Date) || isNaN(date.getTime())) {
-    console.warn('ÔÜá´©Å Data non valida passata a isInVacationPeriod');
+    console.warn('├ó┼í┬á├»┬©┬Å Data non valida passata a isInVacationPeriod');
     return false;
   }
 
@@ -118,11 +118,11 @@ function isInVacationPeriod(date = new Date(), scriptTimeZone = "") {
       try {
         return Utilities.formatDate(source, scriptTimeZone, 'yyyy-MM-dd');
       } catch (e) {
-        console.warn(`ÔÜá´©Å Impossibile applicare timezone script (${scriptTimeZone}) in isInVacationPeriod: ${e.message}`);
+        console.warn(`├ó┼í┬á├»┬©┬Å Impossibile applicare timezone script (${scriptTimeZone}) in isInVacationPeriod: ${e.message}`);
       }
     }
 
-    // Fallback locale: mantiene compatibilit├á anche in test/mocking senza Utilities.
+    // Fallback locale: mantiene compatibilit├â┬á anche in test/mocking senza Utilities.
     const y = source.getFullYear();
     const m = String(source.getMonth() + 1).padStart(2, '0');
     const d = String(source.getDate()).padStart(2, '0');
@@ -166,7 +166,7 @@ function isInSuspensionTime(checkDate = new Date()) {
   let day = now.getDay();
   let hour = now.getHours();
 
-  // Regola di dominio: la sospensione ├¿ ancorata all'orario italiano.
+  // Regola di dominio: la sospensione ├â┬¿ ancorata all'orario italiano.
   const businessTimeZone = 'Europe/Rome';
 
   if (typeof Utilities !== 'undefined' && Utilities && typeof Utilities.formatDate === 'function') {
@@ -178,11 +178,11 @@ function isInSuspensionTime(checkDate = new Date()) {
       day = isNaN(isoDay) ? day : (isoDay % 7);
       hour = parseInt(Utilities.formatDate(now, businessTimeZone, 'H'), 10);
     } catch (e) {
-      console.warn(`ÔÜá´©Å Impossibile applicare timezone business (${businessTimeZone}): ${e.message}`);
+      console.warn(`├ó┼í┬á├»┬©┬Å Impossibile applicare timezone business (${businessTimeZone}): ${e.message}`);
     }
   }
 
-  // 1. GESTIONE FESTIVI (Priorit├á: Sistema ATTIVO)
+  // 1. GESTIONE FESTIVI (Priorit├â┬á: Sistema ATTIVO)
   for (const [hMonth, hDay] of ALWAYS_OPERATING_DAYS) {
     if (monthIndex === hMonth && date === hDay) return false;
   }
@@ -192,7 +192,7 @@ function isInSuspensionTime(checkDate = new Date()) {
   const normalizedNow = new Date(year, monthIndex, date, 12, 0, 0);
   if (isSameCalendarDay(normalizedNow, easter)) return false;
 
-  // Nota manutenzione: base su "easter" ├¿ intenzionale per leggibilit├á semantica.
+  // Nota manutenzione: base su "easter" ├â┬¿ intenzionale per leggibilit├â┬á semantica.
   const pasquetta = new Date(easter);
   pasquetta.setDate(easter.getDate() + 1);
   if (isSameCalendarDay(normalizedNow, pasquetta)) return false;
@@ -276,19 +276,19 @@ function withSheetsRetry(fn, context = 'Operazione Sheets') {
       return fn();
     } catch (error) {
       if (attempt < maxRetries - 1) {
-        console.warn(`ÔÜá´©Å [${context}] Tentativo ${attempt + 1}/${maxRetries} fallito: ${error.message}. Retry in ${1000 * Math.pow(2, attempt)}ms...`);
+        console.warn(`├ó┼í┬á├»┬©┬Å [${context}] Tentativo ${attempt + 1}/${maxRetries} fallito: ${error.message}. Retry in ${1000 * Math.pow(2, attempt)}ms...`);
         Utilities.sleep(1000 * Math.pow(2, attempt));
         continue;
       }
-      console.error(`ÔØî [${context}] Tutti i ${maxRetries} tentativi esauriti. Ultimo errore: ${error.message}`);
+      console.error(`├ó┬Ø┼Æ [${context}] Tutti i ${maxRetries} tentativi esauriti. Ultimo errore: ${error.message}`);
       throw error;
     }
   }
 }
 
 function loadResources(acquireLock = true, hasExternalLock = false) {
-  // ÔÜá´©Å Invariante blindante: niente reload senza lock.
-  // Questo evita race condition in cui due trigger sovrascrivono la cache a met├á serializzazione.
+  // ├ó┼í┬á├»┬©┬Å Invariante blindante: niente reload senza lock.
+  // Questo evita race condition in cui due trigger sovrascrivono la cache a met├â┬á serializzazione.
   if (!acquireLock && !hasExternalLock) {
     throw new Error('loadResources richiede un lock preventivo.');
   }
@@ -296,8 +296,8 @@ function loadResources(acquireLock = true, hasExternalLock = false) {
   if (typeof GLOBAL_CACHE === 'undefined' || !GLOBAL_CACHE) {
     throw new Error('GLOBAL_CACHE non inizializzata: impossibile caricare risorse in sicurezza.');
   }
-  // Nota: da qui in avanti GLOBAL_CACHE ├¿ garantita; evitiamo fallback silenziosi
-  // perch├® maschererebbero regressioni d'inizializzazione del runtime.
+  // Nota: da qui in avanti GLOBAL_CACHE ├â┬¿ garantita; evitiamo fallback silenziosi
+  // perch├â┬® maschererebbero regressioni d'inizializzazione del runtime.
 
   const now = Date.now();
   const cacheIsFresh = GLOBAL_CACHE.loaded && GLOBAL_CACHE.lastLoadedAt && ((now - GLOBAL_CACHE.lastLoadedAt) < RESOURCE_CACHE_TTL_MS);
@@ -313,7 +313,7 @@ function loadResources(acquireLock = true, hasExternalLock = false) {
         if (!GLOBAL_CACHE.loaded) {
           throw new Error('Impossibile acquisire lock per caricamento risorse.');
         }
-        console.warn('ÔÜá´©Å Lock non acquisito ma cache gi├á presente: evito reload concorrente non protetto.');
+        console.warn('├ó┼í┬á├»┬©┬Å Lock non acquisito ma cache gi├â┬á presente: evito reload concorrente non protetto.');
         return;
       }
     }
@@ -327,7 +327,7 @@ function loadResources(acquireLock = true, hasExternalLock = false) {
 function _loadResourcesInternal() {
   const cache = (typeof CacheService !== 'undefined') ? CacheService.getScriptCache() : null;
 
-  // ÔÜá´©Å Scelta blindata: la cache persiste SEMPRE il payload completo delle risorse.
+  // ├ó┼í┬á├»┬©┬Å Scelta blindata: la cache persiste SEMPRE il payload completo delle risorse.
   // Eventuali riduzioni/riassunti vanno fatte solo a runtime nel PromptEngine,
   // mai qui, altrimenti si degrada sistematicamente il caso normale.
 
@@ -339,10 +339,10 @@ function _loadResourcesInternal() {
         const parsedData = _deserializeResourceCache(cachedData);
         Object.assign(GLOBAL_CACHE, parsedData);
         GLOBAL_CACHE.loaded = true;
-        console.log('Ô£ô Risorse caricate dalla Cache veloce.');
+        console.log('├ó┼ôÔÇ£ Risorse caricate dalla Cache veloce.');
         return;
       } catch (e) {
-        console.warn('ÔÜá´©Å Cache corrotta o obsoleta, ricaricamento dai fogli...');
+        console.warn('├ó┼í┬á├»┬©┬Å Cache corrotta o obsoleta, ricaricamento dai fogli...');
       }
     }
   }
@@ -362,8 +362,8 @@ function _loadResourcesInternal() {
     throw new Error('Impossibile aprire il foglio. Verifica CONFIG.SPREADSHEET_ID. Dettaglio: ' + e.message);
   }
 
-  // Hardening: evita crash se CONFIG non ├¿ ancora inizializzato (ordine file GAS)
-  // Nota manutenzione: questo caricamento risorse deve avere priorit├á su processUnreadEmails
+  // Hardening: evita crash se CONFIG non ├â┬¿ ancora inizializzato (ordine file GAS)
+  // Nota manutenzione: questo caricamento risorse deve avere priorit├â┬á su processUnreadEmails
   // per assicurare che KB e Dottrina siano disponibili.
   const cfg = (typeof CONFIG !== 'undefined' && CONFIG) ? CONFIG : {
     KB_SHEET_NAME: 'Istruzioni',
@@ -453,15 +453,15 @@ function _loadResourcesInternal() {
     try {
       const serialized = _serializeResourceCache(newCacheData, false);
       _writeResourceCachePayload(cache, serialized);
-      console.log('Ô£ô Risorse caricate da Fogli e salvate in Cache.');
+      console.log('├ó┼ôÔÇ£ Risorse caricate da Fogli e salvate in Cache.');
     } catch (e) {
-      console.warn('ÔÜá´©Å Salvataggio cache standard fallito: ' + e.message);
+      console.warn('├ó┼í┬á├»┬©┬Å Salvataggio cache standard fallito: ' + e.message);
       try {
         const compressedPayload = _serializeResourceCache(newCacheData, true);
         _writeResourceCachePayload(cache, compressedPayload);
-        console.warn('ÔÜá´©Å Cache risorse salvata in formato compresso (payload vicino limite 100KB).');
+        console.warn('├ó┼í┬á├»┬©┬Å Cache risorse salvata in formato compresso (payload vicino limite 100KB).');
       } catch (compressionError) {
-        console.warn('ÔÜá´©Å Impossibile salvare in cache anche in formato compresso: ' + compressionError.message);
+        console.warn('├ó┼í┬á├»┬©┬Å Impossibile salvare in cache anche in formato compresso: ' + compressionError.message);
       }
     }
   }
@@ -519,7 +519,7 @@ function _splitCachePayload(payload, maxSize) {
 function _readResourceCachePayload(cache) {
   if (!cache) return null;
 
-  // Lettura cache con supporto chiave precedente per continuit├á operativa.
+  // Lettura cache con supporto chiave precedente per continuit├â┬á operativa.
   // Manteniamo questo ramo per non perdere warm-cache durante deploy graduali.
   const v1Payload = cache.get(RESOURCE_CACHE_KEY_V1);
   if (v1Payload) {
@@ -545,7 +545,7 @@ function _readResourceCachePayload(cache) {
   const chunks = cache.getAll(keys);
   const missing = keys.find(k => !chunks[k]);
   if (missing) {
-    console.warn(`ÔÜá´©Å Cache multipart incompleta (${missing}), invalido payload e forzo reload.`);
+    console.warn(`├ó┼í┬á├»┬©┬Å Cache multipart incompleta (${missing}), invalido payload e forzo reload.`);
     _invalidateResourceCacheStorage(cache);
     return null;
   }
@@ -556,7 +556,7 @@ function _readResourceCachePayload(cache) {
 function _writeResourceCachePayload(cache, payload) {
   if (!cache) return;
 
-  // ÔÜá´©Å Prima invalidiamo sempre: evita mix V2-inline + multipart stale dopo downgrade/upgrade.
+  // ├ó┼í┬á├»┬©┬Å Prima invalidiamo sempre: evita mix V2-inline + multipart stale dopo downgrade/upgrade.
   _invalidateResourceCacheStorage(cache);
 
   if (payload.length <= RESOURCE_CACHE_MAX_PART_SIZE) {
@@ -564,7 +564,7 @@ function _writeResourceCachePayload(cache, payload) {
     return;
   }
 
-  // ÔÜá´©Å Multipart ├¿ una protezione tecnica contro il limite CacheService (~100KB/entry),
+  // ├ó┼í┬á├»┬©┬Å Multipart ├â┬¿ una protezione tecnica contro il limite CacheService (~100KB/entry),
   // non una riduzione funzionale della KB.
   const parts = _splitCachePayload(payload, RESOURCE_CACHE_MAX_PART_SIZE);
   if (!parts.length) {
@@ -578,7 +578,7 @@ function _writeResourceCachePayload(cache, payload) {
   values[RESOURCE_CACHE_PARTS_KEY] = String(parts.length);
 
   cache.putAll(values, RESOURCE_CACHE_TTL_SECONDS);
-  console.warn(`ÔÜá´©Å Cache risorse salvata in modalit├á multipart (${parts.length} chunk).`);
+  console.warn(`├ó┼í┬á├»┬©┬Å Cache risorse salvata in modalit├â┬á multipart (${parts.length} chunk).`);
 }
 
 function _invalidateResourceCacheStorage(cache) {
@@ -601,7 +601,7 @@ function _invalidateResourceCacheStorage(cache) {
  * Utile come comando da eseguire a mano dall'editor Apps Script.
  */
 function clearKnowledgeCache() {
-  // ÔÜá´©Å Comando operativo ufficiale: resetta RAM + ScriptCache in modo coerente.
+  // ├ó┼í┬á├»┬©┬Å Comando operativo ufficiale: resetta RAM + ScriptCache in modo coerente.
   // Evitare reset "parziali" altrove: causano stati fantasma e reload intermittenti.
   GLOBAL_CACHE.loaded = false;
   GLOBAL_CACHE.lastLoadedAt = 0;
@@ -626,7 +626,7 @@ function clearKnowledgeCache() {
     // best effort
   }
 
-  console.log('­ƒùæ´©Å Cache conoscenza/config svuotata manualmente (RAM + ScriptCache)');
+  console.log('├░┼©ÔÇöÔÇÿ├»┬©┬Å Cache conoscenza/config svuotata manualmente (RAM + ScriptCache)');
 }
 
 // Alias per invocazione manuale o da trigger precedenti.
@@ -635,18 +635,18 @@ function clearCache() {
 }
 
 /**
- * Forza invalidazione + ricarica immediata usando le funzioni operative gi├á esistenti
+ * Forza invalidazione + ricarica immediata usando le funzioni operative gi├â┬á esistenti
  * (`clearKnowledgeCache`/`clearCache` + `loadResources`).
  * Da usare quando cambia il contenuto dei fogli e non si vuole attendere il TTL.
  */
 function primeCache() {
-  // ÔÜá´©Å Orchestrazione voluta: 1) invalidate totale, 2) reload immediato.
+  // ├ó┼í┬á├»┬©┬Å Orchestrazione voluta: 1) invalidate totale, 2) reload immediato.
   // Non invertire l'ordine (reload->clear) o si ottiene una cache svuotata subito dopo il warm-up.
   clearKnowledgeCache();
   loadResources(true, false);
   const kbSize = (GLOBAL_CACHE.knowledgeBase || '').length;
   const doctrineSize = (GLOBAL_CACHE.doctrineBase || '').length;
-  console.log(`­ƒöä Cache primed manualmente (KB=${kbSize} chars, Dottrina=${doctrineSize} chars).`);
+  console.log(`├░┼©ÔÇØÔÇ× Cache primed manualmente (KB=${kbSize} chars, Dottrina=${doctrineSize} chars).`);
   return {
     loaded: GLOBAL_CACHE.loaded,
     lastLoadedAt: GLOBAL_CACHE.lastLoadedAt,
@@ -720,8 +720,8 @@ function _loadAdvancedConfig(ss) {
     periods.forEach(r => {
       if (r[0] instanceof Date && r[2] instanceof Date) {
         // Nota: NON estendiamo la fine giornata con setHours(23:59:59).
-        // isInVacationPeriod confronta gi├á date normalizzate a "yyyy-MM-dd"
-        // nel timezone business, quindi il giorno finale ├¿ incluso senza manipolazioni aggiuntive.
+        // isInVacationPeriod confronta gi├â┬á date normalizzate a "yyyy-MM-dd"
+        // nel timezone business, quindi il giorno finale ├â┬¿ incluso senza manipolazioni aggiuntive.
         config.vacationPeriods.push({ start: r[0], end: r[2] });
       }
     });
@@ -730,7 +730,7 @@ function _loadAdvancedConfig(ss) {
     const susp = sheet.getRange('B10:E16').getValues();
     susp.forEach((r, i) => {
       // Mapping esplicito indice-riga -> getDay JS:
-      // B10..B16 = Lun..Dom  => 1..6,0 (dove Domenica in JS ├¿ 0, non 7).
+      // B10..B16 = Lun..Dom  => 1..6,0 (dove Domenica in JS ├â┬¿ 0, non 7).
       const day = (i + 1) % 7;
       // B contiene il nome del giorno; gli orari reali sono in C (r[1]) e D (r[2]).
       // Manteniamo questo mapping: usare r[0]/r[2] sarebbe errato per questo layout.
@@ -786,7 +786,7 @@ function setupAllTriggers() {
   // 3. Trigger Metriche/Statistiche (Giornaliero)
   setupMetricsTrigger();
 
-  SpreadsheetApp.getUi().alert('Ô£à Tutti i trigger sono stati riattivati correttamente.');
+  SpreadsheetApp.getUi().alert('├ó┼ôÔÇª Tutti i trigger sono stati riattivati correttamente.');
 }
 
 /**
@@ -853,13 +853,13 @@ function processEmailsMain() {
  * Funzione principale invocata dal trigger temporale (es. ogni 5 min)
  */
 function main() {
-  console.log('­ƒÜÇ Avvio pipeline principale');
+  console.log('├░┼©┼íÔé¼ Avvio pipeline principale');
 
-  // 0. Controllo Preventivo API Avanzate
+  // 0. Controllo Preventivo Gmail Advanced Service
   try {
-    GmailApp.getAliases(); // Probe: verifica che i Servizi Avanzati siano abilitati
+    Gmail.Users.getProfile('me'); // Probe reale: verifica disponibilita Gmail Advanced Service
   } catch (apiError) {
-    console.error(`­ƒÆÑ CRITICO: Servizi Avanzati (Gmail API) non abilitati nel progetto GAS. Impossibile procedere.`);
+    console.error('CRITICO: Gmail Advanced Service non disponibile o non autorizzato. Impossibile procedere.');
     return;
   }
 
@@ -871,8 +871,8 @@ function main() {
     hasExecutionLock = executionLock.tryLock(10000);
     if (!hasExecutionLock) {
       // Nota progettuale: evitiamo di accodare trigger aggiuntivi qui per non creare
-      // una tempesta di trigger concorrenti; il trigger periodico successivo riprover├á.
-      console.warn('ÔÜá´©Å Esecuzione gi├á in corso o lock bloccato. Salto turno.');
+      // una tempesta di trigger concorrenti; il trigger periodico successivo riprover├â┬á.
+      console.warn('├ó┼í┬á├»┬©┬Å Esecuzione gi├â┬á in corso o lock bloccato. Salto turno.');
       return;
     }
 
@@ -880,13 +880,13 @@ function main() {
     withSheetsRetry(() => loadResources(false, true), 'loadResources(main)');
 
     if (!GLOBAL_CACHE.loaded) {
-      console.error('­ƒÆÑ Risorse non caricate correttamente (GLOBAL_CACHE.loaded=false). Interruzione preventiva.');
+      console.error('├░┼©ÔÇÖ┬Ñ Risorse non caricate correttamente (GLOBAL_CACHE.loaded=false). Interruzione preventiva.');
       return;
     }
 
     // 3. Controllo Stato Sistema
     if (!GLOBAL_CACHE.systemEnabled) {
-      console.log('­ƒøæ Sistema disattivato da foglio Controllo.');
+      console.log('├░┼©ÔÇ║ÔÇÿ Sistema disattivato da foglio Controllo.');
       return;
     }
 
@@ -896,10 +896,10 @@ function main() {
         : 12;
 
       if (!hasStaleUnreadThreads(staleHours)) {
-        console.log('­ƒÆñ Sistema in sospensione (orario ufficio/festivit├á).');
+        console.log('├░┼©ÔÇÖ┬ñ Sistema in sospensione (orario ufficio/festivit├â┬á).');
         return;
       }
-      console.warn(`ÔÅ░ Sospensione bypassata: trovate email non lette pi├╣ vecchie di ${staleHours}h.`);
+      console.warn(`├ó┬Å┬░ Sospensione bypassata: trovate email non lette pi├â┬╣ vecchie di ${staleHours}h.`);
     }
 
     // 4. Orchestrazione Pipeline (Delegato alle classi di servizio)
@@ -907,15 +907,15 @@ function main() {
     const knowledgeBase = GLOBAL_CACHE.knowledgeBase || '';
     const doctrineBase = GLOBAL_CACHE.doctrineBase || '';
 
-    // Passaggio della dottrina strutturata e testo piatto per compatibilit├á con i formati di input
+    // Passaggio della dottrina strutturata e testo piatto per compatibilit├â┬á con i formati di input
     const results = processor.processUnreadEmails(knowledgeBase, doctrineBase, true); // true = skip double lock
 
     if (results) {
-      console.log(`­ƒôè Batch completato: ${results.total || 0} analizzati, ${results.replied || 0} risposte, ${results.errors || 0} errori.`);
+      console.log(`├░┼©ÔÇ£┼á Batch completato: ${results.total || 0} analizzati, ${results.replied || 0} risposte, ${results.errors || 0} errori.`);
     }
 
   } catch (error) {
-    console.error(`­ƒÆÑ Errore fatale in main: ${error.message}`);
+    console.error(`├░┼©ÔÇÖ┬Ñ Errore fatale in main: ${error.message}`);
     if (typeof createLogger === 'function') {
       try {
         const logger = createLogger('Main');
diff --git a/scripts/ci_smoke_tests.js b/scripts/ci_smoke_tests.js
index e964de8..30bf982 100644
--- a/scripts/ci_smoke_tests.js
+++ b/scripts/ci_smoke_tests.js
@@ -288,7 +288,7 @@ function testGeminiMalformedJson() {
 
     const mockBadJson = {
         getResponseCode: () => 200,
-        getContentText: () => 'questo non ├¿ JSON valido {{{}'
+        getContentText: () => 'questo non ├âãÆ├é┬¿ JSON valido {{{}'
     };
 
     const service = createMockGeminiService(() => mockBadJson);
@@ -353,7 +353,7 @@ function testResponseValidatorForbiddenContent() {
     const resForbidden = validator._checkForbiddenContent('Le messe sono alle [ORARIO].');
     assert(resForbidden.score === 0.0, 'Contenuto con placeholder deve avere score 0');
 
-    const resClean = validator._checkForbiddenContent('Gentile signora, le confermo che la Santa Messa festiva ├¿ celebrata ogni domenica alle ore 10:00.');
+    const resClean = validator._checkForbiddenContent('Gentile signora, le confermo che la Santa Messa festiva ├âãÆ├é┬¿ celebrata ogni domenica alle ore 10:00.');
     assert(resClean.score === 1.0, `Contenuto pulito: score atteso 1.0, ottenuto ${resClean.score}`);
     assert(resClean.errors.length === 0, 'Contenuto pulito non deve avere errori');
 }
@@ -391,7 +391,7 @@ function testResponseValidatorFrenchLiturgicalGreeting() {
 
     const validator = new ResponseValidator();
     const result = validator._checkTimeBasedGreeting(
-        'Joyeux No├½l! Merci pour votre message et ├á bient├┤t.',
+        'Joyeux No├âãÆ├é┬½l! Merci pour votre message et ├âãÆ├é┬á bient├âãÆ├é┬┤t.',
         'fr'
     );
 
@@ -407,20 +407,20 @@ function testComputeSalutationMode() {
     loadScript('gas_email_processor.js');
     const NOW = new Date('2026-02-15T10:00:00Z');
 
-    // Primo messaggio ÔåÆ full
+    // Primo messaggio ├â┬ó├óÔé¼┬á├óÔé¼Ôäó full
     const first = computeSalutationMode({ isReply: false, messageCount: 1, memoryExists: false, lastUpdated: null });
     assert(first === 'full', `Primo messaggio: atteso "full", ottenuto "${first}"`);
 
-    // Reply con memoria senza timestamp ÔåÆ continuit├á (fallback conservativo)
+    // Reply con memoria senza timestamp ├â┬ó├óÔé¼┬á├óÔé¼Ôäó continuit├âãÆ├é┬á (fallback conservativo)
     const reply = computeSalutationMode({ isReply: true, messageCount: 2, memoryExists: true, lastUpdated: null });
     assert(reply === 'none_or_continuity', `Reply senza timestamp: atteso "none_or_continuity", ottenuto "${reply}"`);
 
-    // Reply dopo 5 giorni ÔåÆ full (nuovo contatto, > 72h)
+    // Reply dopo 5 giorni ├â┬ó├óÔé¼┬á├óÔé¼Ôäó full (nuovo contatto, > 72h)
     const fiveDaysAgo = new Date(NOW.getTime() - 5 * 24 * 60 * 60 * 1000).toISOString();
     const result5d = computeSalutationMode({ isReply: true, messageCount: 3, memoryExists: true, lastUpdated: fiveDaysAgo, now: NOW });
     assert(result5d === 'full', `Reply dopo 5 giorni: atteso "full", ottenuto "${result5d}"`);
 
-    // Reply dopo 4 giorni ÔåÆ soft (ripresa conversazione entro 96h)
+    // Reply dopo 4 giorni ├â┬ó├óÔé¼┬á├óÔé¼Ôäó soft (ripresa conversazione entro 96h)
     const fourDaysAgo = new Date(NOW.getTime() - 4 * 24 * 60 * 60 * 1000).toISOString();
     const result4d = computeSalutationMode({ isReply: true, messageCount: 3, memoryExists: true, lastUpdated: fourDaysAgo, now: NOW });
     assert(result4d === 'soft', `Reply dopo 4 giorni: atteso "soft", ottenuto "${result4d}"`);
@@ -730,7 +730,7 @@ function testAttachmentContextSanitizationFormatting() {
     const input = 'Riga 1\nRiga 2';
     const rendered = engine._renderAttachmentContext(input);
 
-    assert(rendered.includes('ALLEGATI (TESTO OCR/PDF)'), 'Header allegati non trovato');
+    assert(rendered.includes('ALLEGATI (TESTO ESTRATTO)'), 'Header allegati non trovato');
     assert(rendered.includes('Riga 1\nRiga 2'), 'Il contesto allegati deve preservare newline reali');
 }
 
@@ -752,7 +752,7 @@ function testPromptLiteTokenBudget() {
     });
 
     const tokens = engine.estimateTokens(prompt);
-    assert(!prompt.includes('­ƒôÜ ESEMPI CON FORMATTAZIONE CORRETTA'), 'In profilo lite non devono apparire gli esempi estesi');
+    assert(!prompt.includes('├â┬░├à┬©├óÔé¼┼ô├à┬í ESEMPI CON FORMATTAZIONE CORRETTA'), 'In profilo lite non devono apparire gli esempi estesi');
     assert(tokens < 25000, `Prompt lite deve restare compatto, ottenuti ~${tokens} token`);
 }
 
@@ -868,7 +868,7 @@ function runGoldenCases() {
 
     const matchPattern = (value, pattern) => {
         if (pattern === 'emoji raw in output') {
-            return /(\p{Extended_Pictographic}|[ÔÿÇ-Ô×┐])/u.test(value);
+            return /\p{Extended_Pictographic}/u.test(value);
         }
         return new RegExp(pattern, 'iu').test(value);
     };
@@ -1044,7 +1044,7 @@ function testEscapeHtml() {
 
     const xss = escapeHtml('<script>alert("xss")</script>');
     assert(!xss.includes('<script>'), `escapeHtml deve neutralizzare tag script, ottenuto: "${xss}"`);
-    assert(xss.includes('&lt;script&gt;'), 'escapeHtml deve convertire < e > in entit├á');
+    assert(xss.includes('&lt;script&gt;'), 'escapeHtml deve convertire < e > in entit├âãÆ├é┬á');
 
     const amp = escapeHtml('A & B');
     assert(amp === 'A &amp; B', `escapeHtml deve convertire &, ottenuto: "${amp}"`);
@@ -1066,51 +1066,51 @@ function testMarkdownToHtmlNonStringInput() {
 function testSanitizeUrlIPv6() {
     loadScript('gas_gmail_service.js');
 
-    // IPv6 loopback ÔåÆ null
+    // IPv6 loopback ├â┬ó├óÔé¼┬á├óÔé¼Ôäó null
     const ipv6 = sanitizeUrl('http://[::1]/admin');
     assert(ipv6 === null, 'sanitizeUrl deve bloccare IPv6 loopback [::1]');
 
-    // IP decimale ÔåÆ null
+    // IP decimale ├â┬ó├óÔé¼┬á├óÔé¼Ôäó null
     const decimal = sanitizeUrl('http://2130706433/');
     assert(decimal === null, `sanitizeUrl deve bloccare IP decimale, ottenuto: ${decimal}`);
 
-    // userinfo bypass ÔåÆ null
+    // userinfo bypass ├â┬ó├óÔé¼┬á├óÔé¼Ôäó null
     const userinfo = sanitizeUrl('http://localhost@evil.com/path');
     assert(userinfo === null, 'sanitizeUrl deve bloccare userinfo bypass');
 
-    // localhost con trailing dot ÔåÆ null
+    // localhost con trailing dot ├â┬ó├óÔé¼┬á├óÔé¼Ôäó null
     const localhostTrailingDot = sanitizeUrl('http://localhost./admin');
     assert(localhostTrailingDot === null, 'sanitizeUrl deve bloccare localhost con trailing dot');
 
-    // Dotted quad in notazione hex ÔåÆ null
+    // Dotted quad in notazione hex ├â┬ó├óÔé¼┬á├óÔé¼Ôäó null
     const hexDotted = sanitizeUrl('http://0x7f.0x0.0x0.0x1/admin');
     assert(hexDotted === null, `sanitizeUrl deve bloccare dotted-quad hex, ottenuto: ${hexDotted}`);
 
-    // IPv4-mapped IPv6 loopback (forma compressa) ÔåÆ null
+    // IPv4-mapped IPv6 loopback (forma compressa) ├â┬ó├óÔé¼┬á├óÔé¼Ôäó null
     const mappedLoopback = sanitizeUrl('http://[::ffff:127.0.0.1]/admin');
     assert(mappedLoopback === null, `sanitizeUrl deve bloccare IPv4-mapped loopback, ottenuto: ${mappedLoopback}`);
 
-    // IPv4-mapped IPv6 loopback (forma estesa) ÔåÆ null
+    // IPv4-mapped IPv6 loopback (forma estesa) ├â┬ó├óÔé¼┬á├óÔé¼Ôäó null
     const mappedExpanded = sanitizeUrl('http://[0:0:0:0:0:ffff:7f00:1]/admin');
     assert(mappedExpanded === null, `sanitizeUrl deve bloccare IPv4-mapped loopback esteso, ottenuto: ${mappedExpanded}`);
 
-    // IPv6 unique local (ULA) ÔåÆ null
+    // IPv6 unique local (ULA) ├â┬ó├óÔé¼┬á├óÔé¼Ôäó null
     const ula = sanitizeUrl('http://[fd00::1]/admin');
     assert(ula === null, `sanitizeUrl deve bloccare IPv6 ULA, ottenuto: ${ula}`);
 
-    // IPv6 unspecified ÔåÆ null
+    // IPv6 unspecified ├â┬ó├óÔé¼┬á├óÔé¼Ôäó null
     const unspecified = sanitizeUrl('http://[::]/admin');
     assert(unspecified === null, `sanitizeUrl deve bloccare IPv6 unspecified, ottenuto: ${unspecified}`);
 
-    // Carrier-grade NAT ÔåÆ null
+    // Carrier-grade NAT ├â┬ó├óÔé¼┬á├óÔé¼Ôäó null
     const cgnat = sanitizeUrl('http://100.64.0.10/internal');
     assert(cgnat === null, `sanitizeUrl deve bloccare rete CGNAT, ottenuto: ${cgnat}`);
 
-    // IPv6 pubblico compresso ÔåÆ passa
+    // IPv6 pubblico compresso ├â┬ó├óÔé¼┬á├óÔé¼Ôäó passa
     const publicIpv6 = sanitizeUrl('https://[2001:4860:4860::8888]/dns-query');
     assert(publicIpv6 !== null, `sanitizeUrl deve permettere IPv6 pubblico, ottenuto: ${publicIpv6}`);
 
-    // URL legittimo ÔåÆ passa
+    // URL legittimo ├â┬ó├óÔé¼┬á├óÔé¼Ôäó passa
     const legit = sanitizeUrl('https://www.example.com/page');
     assert(legit !== null, 'sanitizeUrl deve permettere URL legittimi');
 }
@@ -1374,8 +1374,8 @@ function testLoadAdvancedConfigStrictSuspensionHours() {
     const ss = { getSheetByName: (name) => (name === 'Controllo' ? sheet : null) };
     const cfg = _loadAdvancedConfig(ss);
 
-    assert(Array.isArray(cfg.suspensionRules[1]), 'Luned├¼ deve includere la fascia oraria valida 08-12');
-    assert(cfg.suspensionRules[1][0][0] === 8 && cfg.suspensionRules[1][0][1] === 12, 'Fascia luned├¼ non parsata correttamente');
+    assert(Array.isArray(cfg.suspensionRules[1]), 'Luned├âãÆ├é┬¼ deve includere la fascia oraria valida 08-12');
+    assert(cfg.suspensionRules[1][0][0] === 8 && cfg.suspensionRules[1][0][1] === 12, 'Fascia luned├âãÆ├é┬¼ non parsata correttamente');
     assert(cfg.suspensionRules[6][0][0] === 9 && cfg.suspensionRules[6][0][1] === 17, 'Valori con spazi devono essere normalizzati (9-17)');
     assert(cfg.suspensionRules[0][0][0] === 7 && cfg.suspensionRules[0][0][1] === 9, 'Domenica valida deve essere mantenuta (7-9)');
     assert(cfg.suspensionRules[2] == null, 'Valori parzialmente numerici (es. 08x) devono essere scartati');
@@ -1443,8 +1443,8 @@ function testPortugueseDetectionRefinement() {
     // Usa l'helper mock per evitare errori di configurazione API Key
     const gemini = createMockGeminiService(() => ({}));
 
-    // Test con keyword "n├úo" (prima confusa con "non" italiano) e punteggiatura portoghese
-    const ptContent = "Ol├í, n├úo recebi o or├ºamento da viatura. Muito obrigado.";
+    // Test con keyword "n├âãÆ├é┬úo" (prima confusa con "non" italiano) e punteggiatura portoghese
+    const ptContent = "Ol├âãÆ├é┬í, n├âãÆ├é┬úo recebi o or├âãÆ├é┬ºamento da viatura. Muito obrigado.";
     const result = gemini.detectEmailLanguage(ptContent, "Reserva");
 
     assert(result.lang === 'pt', `Atteso PT per contenuto portoghese, ottenuto ${result.lang}`);
@@ -1515,15 +1515,15 @@ function main() {
         ['civic deduplication (10A vs 10B)', testCivicDeduplicationExplicit],
         ['civic extraction with slash/dash suffix', testAddressExtractionWithSlashAndDashSuffix],
         // Error Types (classificazione centralizzata)
-        ['classifyError: quota/429 ÔåÆ retryable', testClassifyErrorQuota],
-        ['classifyError: API key/invalid ÔåÆ non-retryable', testClassifyErrorNonRetryable],
-        ['classifyError: timeout/408/econnaborted ÔåÆ retryable', testClassifyErrorTimeoutSignals],
+        ['classifyError: quota/429 ├â┬ó├óÔé¼┬á├óÔé¼Ôäó retryable', testClassifyErrorQuota],
+        ['classifyError: API key/invalid ├â┬ó├óÔé¼┬á├óÔé¼Ôäó non-retryable', testClassifyErrorNonRetryable],
+        ['classifyError: timeout/408/econnaborted ├â┬ó├óÔé¼┬á├óÔé¼Ôäó retryable', testClassifyErrorTimeoutSignals],
         // GeminiService
         ['portuguese special greeting', testPortugueseSpecialGreeting],
         ['gemini DI + mock fetch', testGeminiDependencyInjectionAndMockFetch],
-        ['gemini contract: 429 ÔåÆ errore propagato', testGeminiRetryOn429],
-        ['gemini contract: malformed JSON ÔåÆ errore parse', testGeminiMalformedJson],
-        ['gemini contract: no candidates ÔåÆ errore semantico', testGeminiNoCandidates],
+        ['gemini contract: 429 ├â┬ó├óÔé¼┬á├óÔé¼Ôäó errore propagato', testGeminiRetryOn429],
+        ['gemini contract: malformed JSON ├â┬ó├óÔé¼┬á├óÔé¼Ôäó errore parse', testGeminiMalformedJson],
+        ['gemini contract: no candidates ├â┬ó├óÔé¼┬á├óÔé¼Ôäó errore semantico', testGeminiNoCandidates],
         // ResponseValidator
         ['validator: check lunghezza', testResponseValidatorCheckLength],
         ['validator: contenuto vietato + placeholder', testResponseValidatorForbiddenContent],
@@ -1587,16 +1587,16 @@ function main() {
     console.log(`SUMMARY: ${passed}/${total} passed (min: ${MIN_EXPECTED_TESTS})`);
 
     if (failed > 0) {
-        console.error(`\nÔØî ${failed} test falliti.`);
+        console.error(`\n├â┬ó├é┬Ø├àÔÇÖ ${failed} test falliti.`);
         process.exit(1);
     }
 
     if (total < MIN_EXPECTED_TESTS) {
-        console.error(`\nÔØî Soglia minima non raggiunta: ${total} test eseguiti, minimo atteso ${MIN_EXPECTED_TESTS}.`);
+        console.error(`\n├â┬ó├é┬Ø├àÔÇÖ Soglia minima non raggiunta: ${total} test eseguiti, minimo atteso ${MIN_EXPECTED_TESTS}.`);
         process.exit(1);
     }
 
-    console.log('\nÔ£à Smoke tests completati con successo.');
+    console.log('\n├â┬ó├àÔÇ£├óÔé¼┬ª Smoke tests completati con successo.');
 }
 
 main();
